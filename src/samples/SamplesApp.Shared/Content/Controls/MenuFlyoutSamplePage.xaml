<Page x:Class="Uno.Themes.Samples.Content.Controls.MenuFlyoutSamplePage"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:sample="using:Uno.Themes.Samples"
	  xmlns:smtx="using:ShowMeTheXAML"
	  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<sample:SamplePageLayout>
		<sample:SamplePageLayout.SimpleTemplate>
			<DataTemplate>
				<StackPanel Spacing="8">
					<!-- Basic MenuFlyout -->
					<smtx:XamlDisplay UniqueKey="Simple_MenuFlyout_Basic">
						<Button Content="Open Menu">
							<Button.Flyout>
								<MenuFlyout>
									<MenuFlyoutItem Text="Cut" />
									<MenuFlyoutItem Text="Copy" />
									<MenuFlyoutItem Text="Paste" />
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>

					<!-- With Icons -->
					<smtx:XamlDisplay UniqueKey="Simple_MenuFlyout_WithIcons">
						<Button Content="Edit Menu">
							<Button.Flyout>
								<MenuFlyout>
									<MenuFlyoutItem Text="Cut">
										<MenuFlyoutItem.Icon>
											<SymbolIcon Symbol="Cut" />
										</MenuFlyoutItem.Icon>
									</MenuFlyoutItem>
									<MenuFlyoutItem Text="Copy">
										<MenuFlyoutItem.Icon>
											<SymbolIcon Symbol="Copy" />
										</MenuFlyoutItem.Icon>
									</MenuFlyoutItem>
									<MenuFlyoutItem Text="Paste">
										<MenuFlyoutItem.Icon>
											<SymbolIcon Symbol="Paste" />
										</MenuFlyoutItem.Icon>
									</MenuFlyoutItem>
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>

					<!-- With Separator -->
					<smtx:XamlDisplay UniqueKey="Simple_MenuFlyout_Separator">
						<Button Content="File Menu">
							<Button.Flyout>
								<MenuFlyout>
									<MenuFlyoutItem Text="New" />
									<MenuFlyoutItem Text="Open" />
									<MenuFlyoutItem Text="Save" />
									<MenuFlyoutSeparator />
									<MenuFlyoutItem Text="Exit" />
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>

					<!-- Toggle MenuFlyoutItem -->
					<smtx:XamlDisplay UniqueKey="Simple_MenuFlyout_Toggle">
						<Button Content="View Options">
							<Button.Flyout>
								<MenuFlyout>
									<ToggleMenuFlyoutItem Text="Show Toolbar"
														   IsChecked="True" />
									<ToggleMenuFlyoutItem Text="Show Status Bar"
														   IsChecked="True" />
									<ToggleMenuFlyoutItem Text="Show Ruler" />
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>

					<!-- SubItem (Nested Menu) -->
					<smtx:XamlDisplay UniqueKey="Simple_MenuFlyout_SubItem">
						<Button Content="Format Menu">
							<Button.Flyout>
								<MenuFlyout>
									<MenuFlyoutItem Text="Bold" />
									<MenuFlyoutItem Text="Italic" />
									<MenuFlyoutSubItem Text="Text Color">
										<MenuFlyoutItem Text="Red" />
										<MenuFlyoutItem Text="Green" />
										<MenuFlyoutItem Text="Blue" />
									</MenuFlyoutSubItem>
									<MenuFlyoutSubItem Text="Font Size">
										<MenuFlyoutItem Text="Small" />
										<MenuFlyoutItem Text="Medium" />
										<MenuFlyoutItem Text="Large" />
									</MenuFlyoutSubItem>
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>

					<!-- Disabled Items -->
					<smtx:XamlDisplay UniqueKey="Simple_MenuFlyout_Disabled">
						<Button Content="Edit Menu (Mixed)">
							<Button.Flyout>
								<MenuFlyout>
									<MenuFlyoutItem Text="Undo"
													 IsEnabled="False" />
									<MenuFlyoutItem Text="Redo"
													 IsEnabled="False" />
									<MenuFlyoutSeparator />
									<MenuFlyoutItem Text="Cut" />
									<MenuFlyoutItem Text="Copy" />
									<MenuFlyoutItem Text="Paste" />
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>

					<!-- Radio MenuFlyoutItem -->
					<smtx:XamlDisplay UniqueKey="Simple_MenuFlyout_RadioItems">
						<Button Content="Sort Options">
							<Button.Flyout>
								<MenuFlyout>
									<RadioMenuFlyoutItem Text="Sort by Name" GroupName="SortGroup" IsChecked="True" />
									<RadioMenuFlyoutItem Text="Sort by Date" GroupName="SortGroup" />
									<RadioMenuFlyoutItem Text="Sort by Size" GroupName="SortGroup" />
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>
				</StackPanel>
			</DataTemplate>
		</sample:SamplePageLayout.SimpleTemplate>
		<sample:SamplePageLayout.FluentTemplate>
			<DataTemplate>
				<StackPanel Spacing="8">
					<smtx:XamlDisplay UniqueKey="Fluent_MenuFlyout_Basic"
									  Style="{StaticResource XamlDisplayBelowStyle}">
						<Button Content="Open Menu">
							<Button.Flyout>
								<MenuFlyout MenuFlyoutPresenterStyle="{StaticResource MenuFlyoutPresenterStyle}">
									<MenuFlyoutItem Text="Cut"
													Style="{StaticResource MenuFlyoutItemStyle}" />
									<MenuFlyoutItem Text="Copy"
													Style="{StaticResource MenuFlyoutItemStyle}" />
									<MenuFlyoutItem Text="Paste"
													Style="{StaticResource MenuFlyoutItemStyle}" />
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>
					<smtx:XamlDisplay UniqueKey="Fluent_MenuFlyout_WithIcons"
									  Style="{StaticResource XamlDisplayBelowStyle}">
						<Button Content="Edit Menu">
							<Button.Flyout>
								<MenuFlyout MenuFlyoutPresenterStyle="{StaticResource MenuFlyoutPresenterStyle}">
									<MenuFlyoutItem Text="Cut"
													Style="{StaticResource MenuFlyoutItemStyle}">
										<MenuFlyoutItem.Icon>
											<SymbolIcon Symbol="Cut" />
										</MenuFlyoutItem.Icon>
									</MenuFlyoutItem>
									<MenuFlyoutItem Text="Copy"
													Style="{StaticResource MenuFlyoutItemStyle}">
										<MenuFlyoutItem.Icon>
											<SymbolIcon Symbol="Copy" />
										</MenuFlyoutItem.Icon>
									</MenuFlyoutItem>
									<MenuFlyoutItem Text="Paste"
													Style="{StaticResource MenuFlyoutItemStyle}">
										<MenuFlyoutItem.Icon>
											<SymbolIcon Symbol="Paste" />
										</MenuFlyoutItem.Icon>
									</MenuFlyoutItem>
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>
					<smtx:XamlDisplay UniqueKey="Fluent_MenuFlyout_Separator"
									  Style="{StaticResource XamlDisplayBelowStyle}">
						<Button Content="File Menu">
							<Button.Flyout>
								<MenuFlyout MenuFlyoutPresenterStyle="{StaticResource MenuFlyoutPresenterStyle}">
									<MenuFlyoutItem Text="New"
													Style="{StaticResource MenuFlyoutItemStyle}" />
									<MenuFlyoutItem Text="Open"
													Style="{StaticResource MenuFlyoutItemStyle}" />
									<MenuFlyoutSeparator Style="{StaticResource MenuFlyoutSeparatorStyle}" />
									<MenuFlyoutItem Text="Exit"
													Style="{StaticResource MenuFlyoutItemStyle}" />
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>
					<smtx:XamlDisplay UniqueKey="Fluent_MenuFlyout_Toggle"
									  Style="{StaticResource XamlDisplayBelowStyle}">
						<Button Content="View Options">
							<Button.Flyout>
								<MenuFlyout MenuFlyoutPresenterStyle="{StaticResource MenuFlyoutPresenterStyle}">
									<ToggleMenuFlyoutItem Text="Show Toolbar"
														  IsChecked="True"
														  Style="{StaticResource ToggleMenuFlyoutItemStyle}" />
									<ToggleMenuFlyoutItem Text="Show Status Bar"
														  Style="{StaticResource ToggleMenuFlyoutItemStyle}" />
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>
					<smtx:XamlDisplay UniqueKey="Fluent_MenuFlyout_SubItem"
									  Style="{StaticResource XamlDisplayBelowStyle}">
						<Button Content="Format Menu">
							<Button.Flyout>
								<MenuFlyout MenuFlyoutPresenterStyle="{StaticResource MenuFlyoutPresenterStyle}">
									<MenuFlyoutItem Text="Bold"
													Style="{StaticResource MenuFlyoutItemStyle}" />
									<MenuFlyoutItem Text="Italic"
													Style="{StaticResource MenuFlyoutItemStyle}" />
									<MenuFlyoutSubItem Text="Text Color"
													   Style="{StaticResource MenuFlyoutSubItemStyle}">
										<MenuFlyoutItem Text="Red"
														Style="{StaticResource MenuFlyoutItemStyle}" />
										<MenuFlyoutItem Text="Green"
														Style="{StaticResource MenuFlyoutItemStyle}" />
										<MenuFlyoutItem Text="Blue"
														Style="{StaticResource MenuFlyoutItemStyle}" />
									</MenuFlyoutSubItem>
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>
					<smtx:XamlDisplay UniqueKey="Fluent_MenuFlyout_Radio"
									  Style="{StaticResource XamlDisplayBelowStyle}">
						<Button Content="Sort Options">
							<Button.Flyout>
								<MenuFlyout MenuFlyoutPresenterStyle="{StaticResource MenuFlyoutPresenterStyle}">
									<RadioMenuFlyoutItem Text="Sort by Name"
														 GroupName="FluentSort"
														 IsChecked="True"
														 Style="{StaticResource RadioMenuFlyoutItemStyle}" />
									<RadioMenuFlyoutItem Text="Sort by Date"
														 GroupName="FluentSort"
														 Style="{StaticResource RadioMenuFlyoutItemStyle}" />
								</MenuFlyout>
							</Button.Flyout>
						</Button>
					</smtx:XamlDisplay>
				</StackPanel>
			</DataTemplate>
		</sample:SamplePageLayout.FluentTemplate>
	</sample:SamplePageLayout>
</Page>
