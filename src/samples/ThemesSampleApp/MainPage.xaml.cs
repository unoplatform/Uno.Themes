using Microsoft.UI.Xaml.Controls;

namespace Uno.Themes.WrapperApp;

/// <summary>
/// Hosts the guest-app picker and the guest content region.
/// </summary>
public sealed partial class MainPage : Page
{
	public MainPage()
	{
		this.InitializeComponent();
	}
}
