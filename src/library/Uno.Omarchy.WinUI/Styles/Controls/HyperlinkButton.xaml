<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:not_win="http://uno.ui/not_win"
					mc:Ignorable="d not_win">

	<!--
		flutter_omarchy has no link widget. Modeled on widgets/button.dart OmarchyButtonStyle.bar(AnsiColor.blue)
		(the secondary style is bar(white) with the plain foreground): no border, EdgeInsets.all(12),
		no tint / hover 0.15 / pressed 0.25, disabled foreground dimmed.

		Deviations from the source:
		- No 120 ms transition: states switch instantly.
		- Text content stays underlined (HyperlinkUnderlineVisible, applied by Uno to the ContentPresenter's text)
		  so a link is not distinguished by color alone; a bar button in the source has no underline.
		- The source draws no focus visual for bar buttons; keyboard focus shows the theme's bright stroke.
	-->

	<x:Boolean x:Key="HyperlinkUnderlineVisible">True</x:Boolean>

	<ResourceDictionary.ThemeDictionaries>

		<!-- One palette feeds both theme dictionaries: identical content. -->
		<ResourceDictionary x:Key="Light">

			<!-- bar(blue) -->
			<StaticResource x:Key="HyperlinkButtonBackground" ResourceKey="OmarchyNormalBlueBrush" />
			<StaticResource x:Key="HyperlinkButtonForeground" ResourceKey="OmarchyBrightBlueBrush" />

			<!-- bar(white), plain foreground -->
			<StaticResource x:Key="SecondaryHyperlinkButtonBackground" ResourceKey="OmarchyNormalWhiteBrush" />
			<StaticResource x:Key="SecondaryHyperlinkButtonForeground" ResourceKey="OmarchyForegroundBrush" />
		</ResourceDictionary>

		<ResourceDictionary x:Key="Default">

			<!-- bar(blue) -->
			<StaticResource x:Key="HyperlinkButtonBackground" ResourceKey="OmarchyNormalBlueBrush" />
			<StaticResource x:Key="HyperlinkButtonForeground" ResourceKey="OmarchyBrightBlueBrush" />

			<!-- bar(white), plain foreground -->
			<StaticResource x:Key="SecondaryHyperlinkButtonBackground" ResourceKey="OmarchyNormalWhiteBrush" />
			<StaticResource x:Key="SecondaryHyperlinkButtonForeground" ResourceKey="OmarchyForegroundBrush" />
		</ResourceDictionary>

	</ResourceDictionary.ThemeDictionaries>

	<Style x:Key="OmarchyHyperlinkButtonStyle"
		   TargetType="HyperlinkButton">
		<Setter Property="FontFamily" Value="{ThemeResource BodyLargeFontFamily}" />
		<Setter Property="FontSize" Value="{ThemeResource BodyLargeFontSize}" />
		<Setter Property="FontWeight" Value="{ThemeResource BodyLargeFontWeight}" />
		<Setter Property="MinHeight" Value="{ThemeResource ControlHeightMedium}" />
		<Setter Property="Padding" Value="{ThemeResource Space300Thickness}" />
		<Setter Property="CornerRadius" Value="{ThemeResource OmarchyCornerRadius}" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="HorizontalAlignment" Value="Left" />
		<Setter Property="VerticalAlignment" Value="Center" />
		<Setter Property="HorizontalContentAlignment" Value="Center" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="UseSystemFocusVisuals" Value="False" />
		<Setter Property="Background" Value="{ThemeResource HyperlinkButtonBackground}" />
		<Setter Property="Foreground" Value="{ThemeResource HyperlinkButtonForeground}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="HyperlinkButton">
					<Grid x:Name="Root"
						  Background="Transparent"
						  AutomationProperties.AccessibilityView="Raw">

						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />
								<VisualState x:Name="PointerOver">
									<VisualState.Setters>
										<Setter Target="TintLayer.Opacity" Value="{ThemeResource OmarchyTintOpacity}" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="Pressed">
									<VisualState.Setters>
										<Setter Target="TintLayer.Opacity" Value="{ThemeResource OmarchyTintOpacityPointerOver}" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="ContentPresenter.Opacity" Value="{ThemeResource OmarchyMutedForegroundOpacity}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
							<VisualStateGroup x:Name="FocusStates">
								<VisualState x:Name="Unfocused" />
								<VisualState x:Name="Focused">
									<VisualState.Setters>
										<Setter Target="FocusBorder.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="PointerFocused" />
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<Border x:Name="TintLayer"
								Background="{TemplateBinding Background}"
								CornerRadius="{TemplateBinding CornerRadius}"
								Opacity="0" />

						<Border x:Name="FocusBorder"
								BorderBrush="{TemplateBinding Foreground}"
								BorderThickness="{ThemeResource OmarchyBorderThickness}"
								CornerRadius="{TemplateBinding CornerRadius}"
								Opacity="0"
								IsHitTestVisible="False" />

						<ContentPresenter x:Name="ContentPresenter"
										  Content="{TemplateBinding Content}"
										  ContentTemplate="{TemplateBinding ContentTemplate}"
										  ContentTransitions="{TemplateBinding ContentTransitions}"
										  Foreground="{TemplateBinding Foreground}"
										  Padding="{TemplateBinding Padding}"
										  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
										  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="OmarchySecondaryHyperlinkButtonStyle"
		   TargetType="HyperlinkButton"
		   BasedOn="{StaticResource OmarchyHyperlinkButtonStyle}">
		<Setter Property="Background" Value="{ThemeResource SecondaryHyperlinkButtonBackground}" />
		<Setter Property="Foreground" Value="{ThemeResource SecondaryHyperlinkButtonForeground}" />
	</Style>

</ResourceDictionary>
