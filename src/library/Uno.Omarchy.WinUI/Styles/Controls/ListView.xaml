<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					mc:Ignorable="">

	<!--
		Omarchy — ListView / ListViewItem
		Reproduces flutter_omarchy widgets/tile.dart (OmarchyTile).

		Tile: height = 4 x the 14 px body size (56), horizontal padding 16, body text in the
		foreground color. Hover paints normal.black at 0.5, pressed at 1: the HoverLayer keeps
		one brush and the visual states drive its Opacity. Hover / pressed / selected text is
		selectedText (= accent) and a selected tile switches to the italic face; selection has
		no background of its own. Keyboard focus is rendered like hover (no system focus visual).

		Deviations:
		- No ItemContainerTransitions / VisualTransitions: the source animates every state over
		  120 ms; the theme omits generated durations.
		- The description / leading / trailing slots of the tile are content (DataTemplate)
		  concerns, not template parts. The source has no multi-select visuals either.
	-->

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- Lightweight styling ThemeResource aliases -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<ResourceDictionary.ThemeDictionaries>

		<!-- ─── Light (one palette, both dictionaries) ─── -->
		<ResourceDictionary x:Key="Light">
			<StaticResource x:Key="ListViewBackground" ResourceKey="SystemControlTransparentBrush" />

			<StaticResource x:Key="ListViewItemBackground" ResourceKey="SystemControlTransparentBrush" />
			<StaticResource x:Key="ListViewItemForeground" ResourceKey="OmarchyForegroundBrush" />
			<!-- lighter_background hover layer; opacity comes from OmarchyTileHoverOpacity (hover) / 1 (pressed) -->
			<StaticResource x:Key="ListViewItemBackgroundPointerOver" ResourceKey="OmarchyNormalBlackBrush" />
			<!-- selectedText -->
			<StaticResource x:Key="ListViewItemForegroundPointerOver" ResourceKey="OmarchyAccentBrush" />
			<StaticResource x:Key="ListViewItemForegroundSelected" ResourceKey="OmarchyAccentBrush" />
			<StaticResource x:Key="ListViewItemForegroundDisabled" ResourceKey="OmarchyMutedBrush" />
		</ResourceDictionary>

		<!-- ─── Default ─── -->
		<ResourceDictionary x:Key="Default">
			<StaticResource x:Key="ListViewBackground" ResourceKey="SystemControlTransparentBrush" />

			<StaticResource x:Key="ListViewItemBackground" ResourceKey="SystemControlTransparentBrush" />
			<StaticResource x:Key="ListViewItemForeground" ResourceKey="OmarchyForegroundBrush" />
			<StaticResource x:Key="ListViewItemBackgroundPointerOver" ResourceKey="OmarchyNormalBlackBrush" />
			<StaticResource x:Key="ListViewItemForegroundPointerOver" ResourceKey="OmarchyAccentBrush" />
			<StaticResource x:Key="ListViewItemForegroundSelected" ResourceKey="OmarchyAccentBrush" />
			<StaticResource x:Key="ListViewItemForegroundDisabled" ResourceKey="OmarchyMutedBrush" />
		</ResourceDictionary>

	</ResourceDictionary.ThemeDictionaries>

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- ListViewItem Style (OmarchyTile) -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<Style x:Key="OmarchyListViewItemStyle"
		   TargetType="ListViewItem">

		<Setter Property="FontFamily" Value="{ThemeResource BodyLargeFontFamily}" />
		<Setter Property="FontSize" Value="{ThemeResource BodyLargeFontSize}" />
		<Setter Property="FontWeight" Value="{ThemeResource BodyLargeFontWeight}" />
		<Setter Property="Background" Value="{ThemeResource ListViewItemBackground}" />
		<Setter Property="Foreground" Value="{ThemeResource ListViewItemForeground}" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="MinHeight" Value="{ThemeResource OmarchyTileHeight}" />
		<Setter Property="Padding" Value="{ThemeResource Space400HorizontalThickness}" />
		<Setter Property="CornerRadius" Value="{ThemeResource OmarchyCornerRadius}" />
		<Setter Property="TabNavigation" Value="Local" />
		<Setter Property="IsHoldingEnabled" Value="True" />
		<Setter Property="AllowDrop" Value="False" />
		<Setter Property="UseSystemFocusVisuals" Value="False" />
		<Setter Property="FocusVisualMargin" Value="0" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListViewItem">
					<Grid x:Name="ContentBorder"
						  Background="{TemplateBinding Background}"
						  BorderBrush="{TemplateBinding BorderBrush}"
						  BorderThickness="{TemplateBinding BorderThickness}"
						  CornerRadius="{TemplateBinding CornerRadius}">

						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />

								<VisualState x:Name="PointerOver">
									<VisualState.Setters>
										<Setter Target="HoverLayer.Opacity" Value="{ThemeResource OmarchyTileHoverOpacity}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundPointerOver}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Pressed">
									<VisualState.Setters>
										<Setter Target="HoverLayer.Opacity" Value="1" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundPointerOver}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Selected">
									<VisualState.Setters>
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundSelected}" />
										<Setter Target="ContentPresenter.FontFamily" Value="{ThemeResource OmarchyItalicFontFamily}" />
										<Setter Target="ContentPresenter.FontStyle" Value="Italic" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="PointerOverSelected">
									<VisualState.Setters>
										<Setter Target="HoverLayer.Opacity" Value="{ThemeResource OmarchyTileHoverOpacity}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundSelected}" />
										<Setter Target="ContentPresenter.FontFamily" Value="{ThemeResource OmarchyItalicFontFamily}" />
										<Setter Target="ContentPresenter.FontStyle" Value="Italic" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="PressedSelected">
									<VisualState.Setters>
										<Setter Target="HoverLayer.Opacity" Value="1" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundSelected}" />
										<Setter Target="ContentPresenter.FontFamily" Value="{ThemeResource OmarchyItalicFontFamily}" />
										<Setter Target="ContentPresenter.FontStyle" Value="Italic" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<VisualStateGroup x:Name="DisabledStates">
								<VisualState x:Name="Enabled" />
								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundDisabled}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<!-- Keyboard focus looks like hover (pointer_area.dart exposes hasFocus; the tile has no dedicated focus visual) -->
							<VisualStateGroup x:Name="FocusStates">
								<VisualState x:Name="Unfocused" />
								<VisualState x:Name="PointerFocused" />
								<VisualState x:Name="Focused">
									<VisualState.Setters>
										<Setter Target="HoverLayer.Opacity" Value="{ThemeResource OmarchyTileHoverOpacity}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundPointerOver}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<!-- Tint layer: one brush, opacity per state -->
						<Border x:Name="HoverLayer"
								Background="{ThemeResource ListViewItemBackgroundPointerOver}"
								Opacity="0"
								IsHitTestVisible="False" />

						<ContentPresenter x:Name="ContentPresenter"
										  ContentTransitions="{TemplateBinding ContentTransitions}"
										  ContentTemplate="{TemplateBinding ContentTemplate}"
										  Content="{TemplateBinding Content}"
										  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
										  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
										  Margin="{TemplateBinding Padding}" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- ListView Style -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<Style x:Key="OmarchyListViewStyle"
		   TargetType="ListView">

		<Setter Property="Background" Value="{ThemeResource ListViewBackground}" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="Padding" Value="0" />
		<Setter Property="CornerRadius" Value="{ThemeResource OmarchyCornerRadius}" />
		<Setter Property="IsTabStop" Value="False" />
		<Setter Property="TabNavigation" Value="Once" />
		<Setter Property="IsSwipeEnabled" Value="True" />
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
		<Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled" />
		<Setter Property="ScrollViewer.IsHorizontalRailEnabled" Value="False" />
		<Setter Property="ScrollViewer.VerticalScrollMode" Value="Enabled" />
		<Setter Property="ScrollViewer.IsVerticalRailEnabled" Value="True" />
		<Setter Property="ScrollViewer.ZoomMode" Value="Disabled" />
		<Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="False" />
		<Setter Property="ScrollViewer.BringIntoViewOnFocusChange" Value="True" />
		<Setter Property="ItemContainerStyle" Value="{ThemeResource OmarchyListViewItemStyle}" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListView">
					<Border Background="{TemplateBinding Background}"
							BorderBrush="{TemplateBinding BorderBrush}"
							BorderThickness="{TemplateBinding BorderThickness}"
							CornerRadius="{TemplateBinding CornerRadius}">

						<ScrollViewer x:Name="ScrollViewer"
									  TabNavigation="{TemplateBinding TabNavigation}"
									  HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
									  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
									  IsHorizontalScrollChainingEnabled="{TemplateBinding ScrollViewer.IsHorizontalScrollChainingEnabled}"
									  VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
									  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
									  IsVerticalScrollChainingEnabled="{TemplateBinding ScrollViewer.IsVerticalScrollChainingEnabled}"
									  IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}"
									  IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}"
									  ZoomMode="{TemplateBinding ScrollViewer.ZoomMode}"
									  IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
									  BringIntoViewOnFocusChange="{TemplateBinding ScrollViewer.BringIntoViewOnFocusChange}"
									  AutomationProperties.AccessibilityView="Raw">

							<ItemsPresenter Header="{TemplateBinding Header}"
											HeaderTemplate="{TemplateBinding HeaderTemplate}"
											HeaderTransitions="{TemplateBinding HeaderTransitions}"
											Footer="{TemplateBinding Footer}"
											FooterTemplate="{TemplateBinding FooterTemplate}"
											FooterTransitions="{TemplateBinding FooterTransitions}"
											Padding="{TemplateBinding Padding}" />
						</ScrollViewer>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
