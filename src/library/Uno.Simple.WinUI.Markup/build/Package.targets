<Project>
	<ItemGroup Condition=" ('$(ImplicitUsings)' == 'true' OR '$(ImplicitUsings)' == 'enable') AND '$(UnoDisableSimpleMarkupImplicitUsings)' != 'true'  ">
		<Using Include="Uno.Simple.Markup" />
	</ItemGroup>
</Project>
