<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:um="using:Uno.Simple"
                    xmlns:toolkit="using:Uno.UI.Toolkit"
                    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:android="http://uno.ui/android"
                    xmlns:ios="http://uno.ui/ios"
                    xmlns:uuc="using:Uno.UI.Controls"
                    mc:Ignorable="android ios">

	<!-- Define the base style for Simple CommandBar -->
	<Style x:Key="SimpleBaseCommandBarStyle"
	       TargetType="CommandBar">
		<Setter Property="Background" Value="{ThemeResource SurfaceBrush}" />
		<Setter Property="Foreground" Value="{ThemeResource OnSurfaceBrush}" />
		<Setter Property="HorizontalAlignment" Value="Stretch" />
		<Setter Property="VerticalAlignment" Value="Top" />
		<Setter Property="HorizontalContentAlignment" Value="Left" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
	</Style>

	<!-- Define the main Simple CommandBar Style -->
	<Style x:Key="SimpleCommandBarStyle"
	       TargetType="CommandBar"
	       BasedOn="{StaticResource SimpleBaseCommandBarStyle}" />

	<Style x:Key="SimpleDefaultCommandBarStyle"
	       TargetType="CommandBar"
	       BasedOn="{StaticResource SimpleCommandBarStyle}" />
</ResourceDictionary>
