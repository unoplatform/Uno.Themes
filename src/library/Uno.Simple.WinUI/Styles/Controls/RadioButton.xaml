<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!--
		Simple Design System — RadioButton Style
		Lightweight styling: every visual-state brush is exposed via ThemeResource.
		
		Semantic resource layer:
		RadioButtonOuterEllipseFill*          = Unchecked background  (semantic key)
		RadioButtonOuterEllipseStroke*        = Unchecked border      (semantic key)
		RadioButtonOuterEllipseCheckedFill*   = Checked background    (semantic key)
		RadioButtonOuterEllipseCheckedStroke* = Checked border        (semantic key)
		RadioButtonForeground*                = Label text            (semantic key)
		
		Simple-prefixed aliases → semantic keys are provided for all names.
		
		Templates reference Simple-prefixed keys (SimpleRadioButton*) so that consumers
		can override EITHER the Simple key or the semantic key.
		
		Simple-only keys (no semantic rename):
		SimpleRadioButtonCheckedDotFill* — no Material equivalent
		
		States: Unchecked/Checked × Normal/PointerOver/Pressed/Disabled (CombinedStates)
		SDS source: Radio/radio.css
	-->

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- Lightweight styling ThemeResource aliases -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<ResourceDictionary.ThemeDictionaries>

		<!-- ─── Light Theme ─── -->
		<ResourceDictionary x:Key="Light">

			<!-- ── Unchecked Background — semantic keys (source of truth) ── -->
			<StaticResource x:Key="RadioButtonOuterEllipseFill" ResourceKey="SurfaceBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseFillPointerOver" ResourceKey="SurfaceBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseFillPressed" ResourceKey="PrimaryContainerBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseFillDisabled" ResourceKey="OnSurfaceDisabledBrush" />
			<!-- ── Unchecked Border — semantic keys (source of truth) ── -->
			<StaticResource x:Key="RadioButtonOuterEllipseStroke" ResourceKey="OutlineBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseStrokePointerOver" ResourceKey="OutlineBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseStrokePressed" ResourceKey="OutlineBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseStrokeDisabled" ResourceKey="OutlineDisabledBrush" />
			<!-- ── Checked Background — semantic keys (source of truth) ── -->
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedFill" ResourceKey="PrimaryBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedFillPointerOver" ResourceKey="PrimaryBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedFillPressed" ResourceKey="PrimaryVariantDarkBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedFillDisabled" ResourceKey="OnSurfaceDisabledBrush" />
			<!-- ── Checked Border — semantic keys (source of truth) ── -->
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedStroke" ResourceKey="PrimaryBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedStrokePointerOver" ResourceKey="PrimaryBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedStrokePressed" ResourceKey="PrimaryVariantDarkBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedStrokeDisabled" ResourceKey="OutlineDisabledBrush" />
			<!-- ── Checked Dot Fill (Simple-only — no semantic rename) ── -->
			<!-- ── Foreground (label) — semantic keys (source of truth) ── -->
			<StaticResource x:Key="RadioButtonForeground" ResourceKey="OnSurfaceBrush" />
			<StaticResource x:Key="RadioButtonForegroundPointerOver" ResourceKey="OnSurfaceBrush" />
			<StaticResource x:Key="RadioButtonForegroundPressed" ResourceKey="OnSurfaceBrush" />
			<StaticResource x:Key="RadioButtonForegroundDisabled" ResourceKey="OnSurfaceDisabledBrush" />
		</ResourceDictionary>

		<!-- ─── Dark Theme (Default) ─── -->
		<ResourceDictionary x:Key="Default">

			<!-- ── Unchecked Background — semantic keys (source of truth) ── -->
			<StaticResource x:Key="RadioButtonOuterEllipseFill" ResourceKey="SurfaceBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseFillPointerOver" ResourceKey="SurfaceBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseFillPressed" ResourceKey="PrimaryContainerBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseFillDisabled" ResourceKey="OnSurfaceDisabledBrush" />
			<!-- ── Unchecked Border — semantic keys (source of truth) ── -->
			<StaticResource x:Key="RadioButtonOuterEllipseStroke" ResourceKey="OutlineBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseStrokePointerOver" ResourceKey="OutlineBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseStrokePressed" ResourceKey="OutlineBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseStrokeDisabled" ResourceKey="OutlineDisabledBrush" />
			<!-- ── Checked Background — semantic keys (source of truth) ── -->
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedFill" ResourceKey="PrimaryBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedFillPointerOver" ResourceKey="PrimaryBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedFillPressed" ResourceKey="PrimaryVariantDarkBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedFillDisabled" ResourceKey="OnSurfaceDisabledBrush" />
			<!-- ── Checked Border — semantic keys (source of truth) ── -->
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedStroke" ResourceKey="PrimaryBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedStrokePointerOver" ResourceKey="PrimaryBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedStrokePressed" ResourceKey="PrimaryVariantDarkBrush" />
			<StaticResource x:Key="RadioButtonOuterEllipseCheckedStrokeDisabled" ResourceKey="OutlineDisabledBrush" />
			<!-- ── Checked Dot Fill (Simple-only — no semantic rename) ── -->
			<!-- ── Foreground (label) — semantic keys (source of truth) ── -->
			<StaticResource x:Key="RadioButtonForeground" ResourceKey="OnSurfaceBrush" />
			<StaticResource x:Key="RadioButtonForegroundPointerOver" ResourceKey="OnSurfaceBrush" />
			<StaticResource x:Key="RadioButtonForegroundPressed" ResourceKey="OnSurfaceBrush" />
			<StaticResource x:Key="RadioButtonForegroundDisabled" ResourceKey="OnSurfaceDisabledBrush" />
		</ResourceDictionary>

	</ResourceDictionary.ThemeDictionaries>

	<StaticResource x:Key="SimpleRadioButtonSize" ResourceKey="SimpleSpace400" />
	<StaticResource x:Key="SimpleRadioButtonDotSize" ResourceKey="SimpleSpace200" />
	<StaticResource x:Key="SimpleRadioButtonStrokeThickness" ResourceKey="SimpleStrokeBorder" />


	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- Layout / sizing resources (theme-agnostic) -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<Thickness x:Key="SimpleRadioButtonContentMargin">16,0,0,0</Thickness>
	<GridLength x:Key="SimpleRadioButtonColumnWidth">16</GridLength>

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- SimpleRadioButtonStyle -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<Style x:Key="SimpleRadioButtonStyle"
		   TargetType="RadioButton">
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="Foreground" Value="{ThemeResource RadioButtonForeground}" />
		<Setter Property="BorderBrush" Value="Transparent" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="FontFamily" Value="{ThemeResource BodyLargeFontFamily}" />
		<Setter Property="FontSize" Value="14" />
		<Setter Property="FontWeight" Value="Normal" />
		<Setter Property="HorizontalAlignment" Value="Left" />
		<Setter Property="VerticalAlignment" Value="Center" />
		<Setter Property="HorizontalContentAlignment" Value="Left" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="MinWidth" Value="0" />
		<Setter Property="MinHeight" Value="32" />
		<Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
		<Setter Property="FocusVisualMargin" Value="-3" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="RadioButton">
					<Grid x:Name="RootGrid"
						  Background="{TemplateBinding Background}"
						  BorderBrush="{TemplateBinding BorderBrush}"
						  BorderThickness="{TemplateBinding BorderThickness}">

						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="{StaticResource SimpleRadioButtonColumnWidth}" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>

						<VisualStateManager.VisualStateGroups>

							<!-- ──── CommonStates ──── -->
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />

								<VisualState x:Name="PointerOver">
									<VisualState.Setters>
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource RadioButtonForegroundPointerOver}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Pressed">
									<VisualState.Setters>
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource RadioButtonForegroundPressed}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<!-- Unchecked element disabled colors -->
										<Setter Target="RadioBorder.Fill" Value="{ThemeResource RadioButtonOuterEllipseFillDisabled}" />
										<Setter Target="RadioBorder.Stroke" Value="{ThemeResource RadioButtonOuterEllipseStrokeDisabled}" />
										<!-- Checked element disabled colors -->
										<Setter Target="RadioBorder_Checked.Fill" Value="{ThemeResource RadioButtonOuterEllipseCheckedFillDisabled}" />
										<Setter Target="RadioBorder_Checked.Stroke" Value="{ThemeResource RadioButtonOuterEllipseCheckedStrokeDisabled}" />
										<Setter Target="RadioDot.Fill" Value="{ThemeResource OnSurfaceDisabledBrush}" />
										<!-- Label -->
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource RadioButtonForegroundDisabled}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<!-- ──── CheckStates ──── -->
							<VisualStateGroup x:Name="CheckStates">
								<VisualState x:Name="Checked">
									<VisualState.Setters>
										<Setter Target="RadioBorder_Checked.Opacity" Value="1" />
										<Setter Target="RadioBorder.Opacity" Value="0" />
										<Setter Target="RadioDot.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Unchecked" />

								<VisualState x:Name="Indeterminate" />
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<!-- Radio Circle -->
						<Grid x:Name="RadioContainer"
							  Grid.Column="0"
							  Width="{StaticResource SimpleRadioButtonSize}"
							  Height="{StaticResource SimpleRadioButtonSize}"
							  HorizontalAlignment="Center"
							  VerticalAlignment="Center">

							<!-- Unchecked outer circle -->
							<Ellipse x:Name="RadioBorder"
									 Width="{StaticResource SimpleRadioButtonSize}"
									 Height="{StaticResource SimpleRadioButtonSize}"
									 Fill="{ThemeResource RadioButtonOuterEllipseFill}"
									 Stroke="{ThemeResource RadioButtonOuterEllipseStroke}"
									 StrokeThickness="{StaticResource SimpleRadioButtonStrokeThickness}" />

							<!-- Checked outer circle (hidden by default) -->
							<Ellipse x:Name="RadioBorder_Checked"
									 Width="{StaticResource SimpleRadioButtonSize}"
									 Height="{StaticResource SimpleRadioButtonSize}"
									 Fill="{ThemeResource RadioButtonOuterEllipseCheckedFill}"
									 Stroke="{ThemeResource RadioButtonOuterEllipseCheckedStroke}"
									 StrokeThickness="{StaticResource SimpleRadioButtonStrokeThickness}"
									 Opacity="0" />

							<!-- Inner dot (hidden by default) -->
							<Ellipse x:Name="RadioDot"
									 Width="{StaticResource SimpleRadioButtonDotSize}"
									 Height="{StaticResource SimpleRadioButtonDotSize}"
									 Fill="{ThemeResource OnPrimaryBrush}"
									 HorizontalAlignment="Center"
									 VerticalAlignment="Center"
									 Opacity="0" />
						</Grid>

						<!-- Content (label) -->
						<ContentPresenter x:Name="ContentPresenter"
										  Grid.Column="1"
										  Margin="{StaticResource SimpleRadioButtonContentMargin}"
										  Content="{TemplateBinding Content}"
										  ContentTemplate="{TemplateBinding ContentTemplate}"
										  ContentTransitions="{TemplateBinding ContentTransitions}"
										  Foreground="{TemplateBinding Foreground}"
										  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
										  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
										  AutomationProperties.AccessibilityView="Raw"
										  TextWrapping="Wrap" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
