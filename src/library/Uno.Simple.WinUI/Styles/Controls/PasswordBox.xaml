<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<ResourceDictionary.ThemeDictionaries>
		<ResourceDictionary x:Key="Default">
			<CornerRadius x:Key="SimplePasswordBoxCornerRadius">8</CornerRadius>
			<Thickness x:Key="SimplePasswordBoxBorderThickness">1</Thickness>
			<Thickness x:Key="SimplePasswordBoxPadding">16,0,8,0</Thickness>
			<x:Double x:Key="SimplePasswordBoxMinHeight">40</x:Double>
		</ResourceDictionary>
		<ResourceDictionary x:Key="Light">
			<CornerRadius x:Key="SimplePasswordBoxCornerRadius">8</CornerRadius>
			<Thickness x:Key="SimplePasswordBoxBorderThickness">1</Thickness>
			<Thickness x:Key="SimplePasswordBoxPadding">16,0,8,0</Thickness>
			<x:Double x:Key="SimplePasswordBoxMinHeight">40</x:Double>
		</ResourceDictionary>
	</ResourceDictionary.ThemeDictionaries>

	<Style x:Key="SimplePasswordBoxStyle"
		   TargetType="PasswordBox">
		<Setter Property="Background" Value="{ThemeResource SurfaceBrush}" />
		<Setter Property="Foreground" Value="{ThemeResource OnSurfaceBrush}" />
		<Setter Property="BorderBrush" Value="{ThemeResource OutlineBrush}" />
		<Setter Property="BorderThickness" Value="{ThemeResource SimplePasswordBoxBorderThickness}" />
		<Setter Property="CornerRadius" Value="{ThemeResource SimplePasswordBoxCornerRadius}" />
		<Setter Property="Padding" Value="{ThemeResource SimplePasswordBoxPadding}" />
		<Setter Property="MinHeight" Value="{ThemeResource SimplePasswordBoxMinHeight}" />
		<Setter Property="FontFamily" Value="{ThemeResource SimpleFontFamily}" />
		<Setter Property="FontSize" Value="16" />
		<Setter Property="FontWeight" Value="Normal" />
		<Setter Property="HorizontalAlignment" Value="Stretch" />
		<Setter Property="VerticalAlignment" Value="Center" />
		<Setter Property="HorizontalContentAlignment" Value="Left" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="UseSystemFocusVisuals" Value="True" />
		<Setter Property="FocusVisualMargin" Value="-3" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="PasswordBox">
					<Grid>
						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />

								<VisualState x:Name="PointerOver">
									<VisualState.Setters>
										<Setter Target="BorderElement.BorderBrush" Value="{ThemeResource OutlineVariantBrush}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Focused">
									<VisualState.Setters>
										<Setter Target="BorderElement.BorderBrush" Value="{ThemeResource PrimaryBrush}" />
										<Setter Target="BorderElement.BorderThickness" Value="2" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="BorderElement.BorderBrush" Value="{ThemeResource OnDisabledBrush}" />
										<Setter Target="BorderElement.Background" Value="{ThemeResource DisabledBrush}" />
										<Setter Target="ContentElement.Foreground" Value="{ThemeResource OnDisabledBrush}" />
										<Setter Target="PlaceholderTextContentPresenter.Foreground" Value="{ThemeResource OnDisabledBrush}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<VisualStateGroup x:Name="ButtonStates">
								<VisualState x:Name="ButtonVisible">
									<VisualState.Setters>
										<Setter Target="RevealButton.Visibility" Value="Visible" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="ButtonCollapsed" />
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<Border x:Name="BorderElement"
								Background="{TemplateBinding Background}"
								BorderBrush="{TemplateBinding BorderBrush}"
								BorderThickness="{TemplateBinding BorderThickness}"
								CornerRadius="{TemplateBinding CornerRadius}"
								MinHeight="{TemplateBinding MinHeight}">

							<Grid VerticalAlignment="Center">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="Auto" />
								</Grid.ColumnDefinitions>

								<Grid Grid.Column="0"
									  Padding="{TemplateBinding Padding}">
									<ScrollViewer x:Name="ContentElement"
												  HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
												  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
												  VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
												  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
												  VerticalAlignment="Center"
												  IsHorizontalRailEnabled="False"
												  IsVerticalRailEnabled="False"
												  IsTabStop="False"
												  ZoomMode="Disabled"
												  AutomationProperties.AccessibilityView="Raw" />
									<TextBlock x:Name="PlaceholderTextContentPresenter"
											   Text="{TemplateBinding PlaceholderText}"
											   Foreground="{ThemeResource OnSurfaceMediumBrush}"
											   IsHitTestVisible="False"
											   VerticalAlignment="Center" />
								</Grid>

								<Button x:Name="RevealButton"
										Grid.Column="1"
										Visibility="Collapsed"
										Background="Transparent"
										BorderThickness="0"
										Padding="8,0"
										MinWidth="32"
										MinHeight="32"
										VerticalAlignment="Center"
										Foreground="{ThemeResource OnSurfaceMediumBrush}">
									<FontIcon Glyph="&#xE052;"
											  FontSize="16"
											  Foreground="{ThemeResource OnSurfaceMediumBrush}" />
								</Button>
							</Grid>
						</Border>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="SimpleDefaultPasswordBoxStyle"
		   TargetType="PasswordBox"
		   BasedOn="{StaticResource SimplePasswordBoxStyle}" />
</ResourceDictionary>
