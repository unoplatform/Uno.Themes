<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!--
		Simple Design System — ToggleSwitch Style
		Lightweight styling: every visual-state brush is exposed via ThemeResource.
		
		Templates reference Simple-prefixed keys (SimpleToggleSwitch*) which alias
		to the semantic keys (ToggleSwitchOuterBorder*, ToggleSwitchKnobBounds*,
		ToggleSwitchKnob*, ToggleSwitchContent*).  Consumers can override EITHER key.
		
		Backwards-compat aliases are provided for all old Simple-prefixed names.
		
		States: CommonStates (Normal/PointerOver/Pressed/Disabled) x
		ToggleStates (Off/On/Dragging)
		SDS source: Switch/switch.css
	-->

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- Lightweight styling ThemeResource aliases -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<ResourceDictionary.ThemeDictionaries>

		<!-- ─── Light Theme ─── -->
		<ResourceDictionary x:Key="Light">

			<!-- ── Off-track — semantic keys (source of truth) ── -->
			<StaticResource x:Key="ToggleSwitchOuterBorderFill" ResourceKey="SurfaceVariantBrush" />
			<StaticResource x:Key="ToggleSwitchOuterBorderStroke" ResourceKey="OutlineBrush" />
			<StaticResource x:Key="ToggleSwitchOuterBorderFillPressed" ResourceKey="TertiaryContainerBrush" />
			<StaticResource x:Key="ToggleSwitchOuterBorderFillDisabled" ResourceKey="OnSurfaceDisabledBrush" />
			<StaticResource x:Key="ToggleSwitchOuterBorderStrokeDisabled" ResourceKey="OutlineDisabledBrush" />
			<!-- ── On-track — semantic keys (source of truth) ── -->
			<StaticResource x:Key="ToggleSwitchKnobBoundsFill" ResourceKey="PrimaryBrush" />
			<StaticResource x:Key="ToggleSwitchKnobBoundsFillPressed" ResourceKey="PrimaryVariantDarkBrush" />
			<StaticResource x:Key="ToggleSwitchKnobBoundsFillDisabled" ResourceKey="OnSurfaceDisabledBrush" />
			<!-- ── Thumb — semantic keys (source of truth) ── -->
			<StaticResource x:Key="ToggleSwitchKnobOffFill" ResourceKey="OnSurfaceMediumBrush" />
			<StaticResource x:Key="ToggleSwitchKnobOnFill" ResourceKey="OnPrimaryBrush" />
			<StaticResource x:Key="ToggleSwitchKnobFillDisabled" ResourceKey="OnSurfaceDisabledBrush" />
			<!-- ── Label — semantic keys (source of truth) ── -->
			<StaticResource x:Key="ToggleSwitchContentForeground" ResourceKey="OnSurfaceBrush" />
			<StaticResource x:Key="ToggleSwitchContentForegroundDisabled" ResourceKey="OnSurfaceDisabledBrush" />
		</ResourceDictionary>

		<!-- ─── Dark Theme (Default) ─── -->
		<ResourceDictionary x:Key="Default">

			<!-- ── Off-track — semantic keys (source of truth) ── -->
			<StaticResource x:Key="ToggleSwitchOuterBorderFill" ResourceKey="SurfaceVariantBrush" />
			<StaticResource x:Key="ToggleSwitchOuterBorderStroke" ResourceKey="OutlineBrush" />
			<StaticResource x:Key="ToggleSwitchOuterBorderFillPressed" ResourceKey="TertiaryContainerBrush" />
			<StaticResource x:Key="ToggleSwitchOuterBorderFillDisabled" ResourceKey="OnSurfaceDisabledBrush" />
			<StaticResource x:Key="ToggleSwitchOuterBorderStrokeDisabled" ResourceKey="OutlineDisabledBrush" />
			<!-- ── On-track — semantic keys (source of truth) ── -->
			<StaticResource x:Key="ToggleSwitchKnobBoundsFill" ResourceKey="PrimaryBrush" />
			<StaticResource x:Key="ToggleSwitchKnobBoundsFillPressed" ResourceKey="PrimaryVariantDarkBrush" />
			<StaticResource x:Key="ToggleSwitchKnobBoundsFillDisabled" ResourceKey="OnSurfaceDisabledBrush" />
			<!-- ── Thumb — semantic keys (source of truth) ── -->
			<StaticResource x:Key="ToggleSwitchKnobOffFill" ResourceKey="OnSurfaceMediumBrush" />
			<StaticResource x:Key="ToggleSwitchKnobOnFill" ResourceKey="OnPrimaryBrush" />
			<StaticResource x:Key="ToggleSwitchKnobFillDisabled" ResourceKey="OnSurfaceDisabledBrush" />
			<!-- ── Label — semantic keys (source of truth) ── -->
			<StaticResource x:Key="ToggleSwitchContentForeground" ResourceKey="OnSurfaceBrush" />
			<StaticResource x:Key="ToggleSwitchContentForegroundDisabled" ResourceKey="OnSurfaceDisabledBrush" />
		</ResourceDictionary>

	</ResourceDictionary.ThemeDictionaries>

	<StaticResource x:Key="SimpleToggleSwitchTrackWidth" ResourceKey="SimpleIconLarge" /> <!-- sds-size-icon-large -->
	<StaticResource x:Key="SimpleToggleSwitchTrackHeight" ResourceKey="SimpleIconSmall" /> <!-- sds-size-icon-small -->
	<StaticResource x:Key="SimpleToggleSwitchStrokeThickness" ResourceKey="SimpleStrokeBorder" /> <!-- sds-size-stroke-border -->
	<StaticResource x:Key="SimpleToggleSwitchThumbSize" ResourceKey="SimpleSpace400" />
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- Layout / sizing resources (theme-agnostic) -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<x:Double x:Key="SimpleToggleSwitchTrackRadius">12</x:Double>
	<Thickness x:Key="SimpleToggleSwitchThumbOffMargin">4,0,0,0</Thickness>
	<Thickness x:Key="SimpleToggleSwitchThumbOnMargin">20,0,0,0</Thickness>
	<Thickness x:Key="SimpleToggleSwitchHeaderMargin">0,0,0,4</Thickness>

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- SimpleToggleSwitchStyle -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<Style x:Key="SimpleToggleSwitchStyle"
		   TargetType="ToggleSwitch">
		<Setter Property="FontFamily" Value="{ThemeResource BodyMediumFontFamily}" />
		<Setter Property="FontSize" Value="{ThemeResource BodyMediumFontSize}" />
		<Setter Property="Foreground" Value="{ThemeResource ToggleSwitchContentForeground}" />
		<Setter Property="HorizontalAlignment" Value="Left" />
		<Setter Property="VerticalAlignment" Value="Center" />
		<Setter Property="HorizontalContentAlignment" Value="Left" />
		<Setter Property="MinWidth" Value="{ThemeResource SimpleIconLarge}" />
		<Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
		<Setter Property="FocusVisualMargin" Value="-3" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ToggleSwitch">
					<Grid>
						<VisualStateManager.VisualStateGroups>

							<!--
								ToggleStates MUST be declared first so CommonStates (declared second)
								can override colors in the Disabled state.
							-->
							<VisualStateGroup x:Name="ToggleStates">
								<VisualState x:Name="Dragging" />

								<VisualState x:Name="Off">
									<VisualState.Setters>
										<Setter Target="OuterBorder.Opacity" Value="1" />
										<Setter Target="SwitchKnobBounds.Opacity" Value="0" />
										<Setter Target="KnobOff.Opacity" Value="1" />
										<Setter Target="KnobOn.Opacity" Value="0" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="On">
									<VisualState.Setters>
										<Setter Target="OuterBorder.Opacity" Value="0" />
										<Setter Target="SwitchKnobBounds.Opacity" Value="1" />
										<Setter Target="KnobOff.Opacity" Value="0" />
										<Setter Target="KnobOn.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<!--
								CommonStates declared AFTER ToggleStates.
								Disabled setters override ToggleStates colors.
							-->
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />
								<VisualState x:Name="PointerOver" />

								<VisualState x:Name="Pressed">
									<VisualState.Setters>
										<Setter Target="OuterBorder.Fill" Value="{ThemeResource ToggleSwitchOuterBorderFillPressed}" />
										<Setter Target="SwitchKnobBounds.Fill" Value="{ThemeResource ToggleSwitchKnobBoundsFillPressed}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="OuterBorder.Fill" Value="{ThemeResource ToggleSwitchOuterBorderFillDisabled}" />
										<Setter Target="OuterBorder.Stroke" Value="{ThemeResource ToggleSwitchOuterBorderStrokeDisabled}" />
										<Setter Target="SwitchKnobBounds.Fill" Value="{ThemeResource ToggleSwitchKnobBoundsFillDisabled}" />
										<Setter Target="KnobOff.Fill" Value="{ThemeResource ToggleSwitchKnobFillDisabled}" />
										<Setter Target="KnobOn.Fill" Value="{ThemeResource ToggleSwitchKnobFillDisabled}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ToggleSwitchContentForegroundDisabled}" />
										<Setter Target="HeaderContentPresenter.Foreground" Value="{ThemeResource ToggleSwitchContentForegroundDisabled}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

						</VisualStateManager.VisualStateGroups>

						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
						</Grid.RowDefinitions>

						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*" />
							<ColumnDefinition Width="Auto" />
						</Grid.ColumnDefinitions>

						<!-- Header -->
						<ContentPresenter x:Name="HeaderContentPresenter"
										  Grid.Row="0"
										  Grid.Column="0"
										  Content="{TemplateBinding Header}"
										  ContentTemplate="{TemplateBinding HeaderTemplate}"
										  Foreground="{ThemeResource ToggleSwitchContentForeground}"
										  Margin="{StaticResource SimpleToggleSwitchHeaderMargin}"
										  VerticalAlignment="Top"
										  Visibility="Collapsed" />

						<!-- Track & Knob -->
						<Grid Grid.Row="1"
							  Grid.Column="0"
							  HorizontalAlignment="Left"
							  VerticalAlignment="Top"
							  Width="{StaticResource SimpleToggleSwitchTrackWidth}"
							  Height="{StaticResource SimpleToggleSwitchTrackHeight}">

							<!-- Off Track -->
							<Rectangle x:Name="OuterBorder"
									   Width="{StaticResource SimpleToggleSwitchTrackWidth}"
									   Height="{StaticResource SimpleToggleSwitchTrackHeight}"
									   RadiusX="{StaticResource SimpleToggleSwitchTrackRadius}"
									   RadiusY="{StaticResource SimpleToggleSwitchTrackRadius}"
									   Fill="{ThemeResource ToggleSwitchOuterBorderFill}"
									   Stroke="{ThemeResource ToggleSwitchOuterBorderStroke}"
									   StrokeThickness="{StaticResource SimpleToggleSwitchStrokeThickness}"
									   HorizontalAlignment="Center" />

							<!-- On Track -->
							<Rectangle x:Name="SwitchKnobBounds"
									   Width="{StaticResource SimpleToggleSwitchTrackWidth}"
									   Height="{StaticResource SimpleToggleSwitchTrackHeight}"
									   RadiusX="{StaticResource SimpleToggleSwitchTrackRadius}"
									   RadiusY="{StaticResource SimpleToggleSwitchTrackRadius}"
									   Fill="{ThemeResource ToggleSwitchKnobBoundsFill}"
									   HorizontalAlignment="Center"
									   Opacity="0" />

							<Grid x:Name="SwitchKnob"
								  HorizontalAlignment="Left"
								  Width="{StaticResource SimpleToggleSwitchTrackWidth}"
								  Height="{StaticResource SimpleToggleSwitchTrackHeight}">

								<!-- On Thumb (right position) -->
								<Grid x:Name="SwitchKnobOn"
									  HorizontalAlignment="Left">
									<Ellipse x:Name="KnobOn"
											 Width="{StaticResource SimpleToggleSwitchThumbSize}"
											 Height="{StaticResource SimpleToggleSwitchThumbSize}"
											 Fill="{ThemeResource ToggleSwitchKnobOnFill}"
											 Margin="{StaticResource SimpleToggleSwitchThumbOnMargin}"
											 HorizontalAlignment="Left"
											 VerticalAlignment="Center"
											 Opacity="0" />
								</Grid>

								<!-- Off Thumb (left position) -->
								<Grid x:Name="SwitchKnobOff"
									  HorizontalAlignment="Left">
									<Ellipse x:Name="KnobOff"
											 Width="{StaticResource SimpleToggleSwitchThumbSize}"
											 Height="{StaticResource SimpleToggleSwitchThumbSize}"
											 Fill="{ThemeResource ToggleSwitchKnobOffFill}"
											 Margin="{StaticResource SimpleToggleSwitchThumbOffMargin}"
											 HorizontalAlignment="Left"
											 VerticalAlignment="Center" />
								</Grid>
							</Grid>

							<!-- SwitchThumb for dragging -->
							<Thumb x:Name="SwitchThumb"
								   AutomationProperties.AccessibilityView="Raw">
								<Thumb.Template>
									<ControlTemplate TargetType="Thumb">
										<Rectangle Fill="Transparent" />
									</ControlTemplate>
								</Thumb.Template>
							</Thumb>
						</Grid>

						<!-- Content -->
						<ContentPresenter x:Name="ContentPresenter"
										  Grid.Row="1"
										  Grid.Column="0"
										  Grid.ColumnSpan="2"
										  Foreground="{TemplateBinding Foreground}"
										  HorizontalAlignment="Left"
										  VerticalAlignment="Center"
										  AutomationProperties.AccessibilityView="Raw" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
