<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!-- TextBox Resources -->
	<CornerRadius x:Key="TextBoxCornerRadius">8</CornerRadius>
	<CornerRadius x:Key="FilledTextBoxCornerRadius">8,8,0,0</CornerRadius>
	<Thickness x:Key="TextBoxBorderThickness">1</Thickness>
	<Thickness x:Key="FilledTextBoxBorderThickness">0,0,0,1</Thickness>
	<Thickness x:Key="TextBoxPadding">12</Thickness>
	<x:Double x:Key="TextBoxHeight">40</x:Double>
	<x:Double x:Key="TextBoxHeightSmall">32</x:Double>

	<!-- SimpleFilledTextBoxStyle - Default filled input with bottom border only -->
	<Style x:Key="SimpleFilledTextBoxStyle" TargetType="TextBox">
		<Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
		<Setter Property="Foreground" Value="{StaticResource OnSurfaceBrush}" />
		<Setter Property="BorderBrush" Value="{StaticResource OutlineBrush}" />
		<Setter Property="BorderThickness" Value="{StaticResource FilledTextBoxBorderThickness}" />
		<Setter Property="CornerRadius" Value="{StaticResource FilledTextBoxCornerRadius}" />
		<Setter Property="Padding" Value="0" />
		<Setter Property="MinHeight" Value="{StaticResource TextBoxHeight}" />
		<Setter Property="FontFamily" Value="{StaticResource SimpleFontFamily}" />
		<Setter Property="FontSize" Value="16" />
		<Setter Property="FontWeight" Value="Normal" />
		<Setter Property="HorizontalAlignment" Value="Stretch" />
		<Setter Property="VerticalAlignment" Value="Center" />
		<Setter Property="HorizontalContentAlignment" Value="Left" />
		<Setter Property="VerticalContentAlignment" Value="Top" />
		<Setter Property="UseSystemFocusVisuals" Value="True" />
		<Setter Property="FocusVisualMargin" Value="-3" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="TextBox">
					<Grid>
						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />
								
								<VisualState x:Name="PointerOver">
									<VisualState.Setters>
										<Setter Target="BorderElement.BorderBrush" Value="{StaticResource OutlineVariantBrush}" />
									</VisualState.Setters>
								</VisualState>
								
								<VisualState x:Name="Focused">
									<VisualState.Setters>
										<Setter Target="BorderElement.BorderBrush" Value="{StaticResource PrimaryBrush}" />
										<Setter Target="BorderElement.BorderThickness" Value="2" />
									</VisualState.Setters>
								</VisualState>
								
								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="BorderElement.Background" Value="{StaticResource DisabledBrush}" />
										<Setter Target="BorderElement.BorderBrush" Value="{StaticResource OnDisabledBrush}" />
										<Setter Target="ContentElement.Foreground" Value="{StaticResource OnDisabledBrush}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>
						
						<Border x:Name="BorderElement"
								Background="{TemplateBinding Background}"
								BorderBrush="{TemplateBinding BorderBrush}"
								BorderThickness="{TemplateBinding BorderThickness}"
								CornerRadius="{TemplateBinding CornerRadius}">
							
							<Border Padding="{StaticResource TextBoxPadding}"
									VerticalAlignment="Top">
								<ScrollViewer x:Name="ContentElement"
											  HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
											  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
											  VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
											  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
											  VerticalAlignment="Top"
											  IsHorizontalRailEnabled="False"
											  IsVerticalRailEnabled="False"
											  IsTabStop="False"
											  ZoomMode="Disabled"
											  AutomationProperties.AccessibilityView="Raw" />
							</Border>
						</Border>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- SimpleOutlinedTextBoxStyle - Outlined input with transparent background and full border -->
	<Style x:Key="SimpleOutlinedTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource SimpleFilledTextBoxStyle}">
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="BorderThickness" Value="{StaticResource TextBoxBorderThickness}" />
		<Setter Property="CornerRadius" Value="{StaticResource TextBoxCornerRadius}" />
	</Style>

	<!-- Small Size Variant -->
	<Style x:Key="SimpleFilledTextBoxSmallStyle" TargetType="TextBox" BasedOn="{StaticResource SimpleFilledTextBoxStyle}">
		<Setter Property="MinHeight" Value="{StaticResource TextBoxHeightSmall}" />
	</Style>

	<!-- Default TextBox Style -->
	<Style TargetType="TextBox" BasedOn="{StaticResource SimpleFilledTextBoxStyle}" />

</ResourceDictionary>
