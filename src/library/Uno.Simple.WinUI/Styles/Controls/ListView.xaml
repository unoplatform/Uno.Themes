<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					mc:Ignorable="">

	<!--
		Simple Design System — ListView / ListViewItem Style
		Maps SDS ListBox component tokens to WinUI ListView + ListViewItem.

		Lightweight styling pattern: every visual-state brush is exposed via
		ThemeResource so consumers can override any individual key.

		Semantic resource layer:
		ListView*       = semantic keys for the container (source of truth)
		ListViewItem*   = semantic keys for list items (source of truth)

		SDS ListBox:
		Container: bg = background-default-default, border = border-default-default, radius = 8px
		Item normal: transparent bg, text = text-default-default, description = text-default-secondary
		Item focused/selected: bg = background-brand-default, text = text-brand-on-brand
		Item disabled: text = text-disabled-default
	-->

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- Lightweight styling ThemeResource aliases -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<ResourceDictionary.ThemeDictionaries>

		<!-- ─── Light Theme ─── -->
		<ResourceDictionary x:Key="Light">

			<!-- ── ListView container — semantic keys (source of truth) ── -->
			<StaticResource x:Key="ListViewBackground" ResourceKey="SurfaceBrush" />
			<StaticResource x:Key="ListViewBorderBrush" ResourceKey="OutlineBrush" />

			<!-- ── ListViewItem — semantic keys (source of truth) ── -->

			<!-- Normal -->
			<StaticResource x:Key="ListViewItemBackground" ResourceKey="SystemControlTransparentBrush" />
			<StaticResource x:Key="ListViewItemForeground" ResourceKey="OnSurfaceBrush" />

			<!-- PointerOver -->
			<StaticResource x:Key="ListViewItemBackgroundPointerOver" ResourceKey="SurfaceVariantBrush" />
			<StaticResource x:Key="ListViewItemForegroundPointerOver" ResourceKey="OnSurfaceBrush" />

			<!-- Pressed -->
			<StaticResource x:Key="ListViewItemBackgroundPressed" ResourceKey="OutlineBrush" />
			<StaticResource x:Key="ListViewItemForegroundPressed" ResourceKey="OnSurfaceBrush" />

			<!-- Selected -->
			<StaticResource x:Key="ListViewItemBackgroundSelected" ResourceKey="PrimaryBrush" />
			<StaticResource x:Key="ListViewItemForegroundSelected" ResourceKey="OnPrimaryBrush" />

			<!-- PointerOverSelected -->
			<StaticResource x:Key="ListViewItemBackgroundPointerOverSelected" ResourceKey="PrimaryVariantDarkBrush" />
			<StaticResource x:Key="ListViewItemForegroundPointerOverSelected" ResourceKey="OnPrimaryBrush" />

			<!-- PressedSelected -->
			<StaticResource x:Key="ListViewItemBackgroundPressedSelected" ResourceKey="PrimaryVariantDarkBrush" />
			<StaticResource x:Key="ListViewItemForegroundPressedSelected" ResourceKey="OnPrimaryBrush" />

			<!-- Disabled -->
			<StaticResource x:Key="ListViewItemForegroundDisabled" ResourceKey="OnSurfaceDisabledBrush" />
		</ResourceDictionary>

		<!-- ─── Dark Theme (Default) ─── -->
		<ResourceDictionary x:Key="Default">

			<!-- ── ListView container — semantic keys (source of truth) ── -->
			<StaticResource x:Key="ListViewBackground" ResourceKey="SurfaceBrush" />
			<StaticResource x:Key="ListViewBorderBrush" ResourceKey="OutlineBrush" />

			<!-- ── ListViewItem — semantic keys (source of truth) ── -->

			<!-- Normal -->
			<StaticResource x:Key="ListViewItemBackground" ResourceKey="SystemControlTransparentBrush" />
			<StaticResource x:Key="ListViewItemForeground" ResourceKey="OnSurfaceBrush" />

			<!-- PointerOver -->
			<StaticResource x:Key="ListViewItemBackgroundPointerOver" ResourceKey="SurfaceVariantBrush" />
			<StaticResource x:Key="ListViewItemForegroundPointerOver" ResourceKey="OnSurfaceBrush" />

			<!-- Pressed -->
			<StaticResource x:Key="ListViewItemBackgroundPressed" ResourceKey="OutlineBrush" />
			<StaticResource x:Key="ListViewItemForegroundPressed" ResourceKey="OnSurfaceBrush" />

			<!-- Selected -->
			<StaticResource x:Key="ListViewItemBackgroundSelected" ResourceKey="PrimaryBrush" />
			<StaticResource x:Key="ListViewItemForegroundSelected" ResourceKey="OnPrimaryBrush" />

			<!-- PointerOverSelected -->
			<StaticResource x:Key="ListViewItemBackgroundPointerOverSelected" ResourceKey="PrimaryVariantDarkBrush" />
			<StaticResource x:Key="ListViewItemForegroundPointerOverSelected" ResourceKey="OnPrimaryBrush" />

			<!-- PressedSelected -->
			<StaticResource x:Key="ListViewItemBackgroundPressedSelected" ResourceKey="PrimaryVariantDarkBrush" />
			<StaticResource x:Key="ListViewItemForegroundPressedSelected" ResourceKey="OnPrimaryBrush" />

			<!-- Disabled -->
			<StaticResource x:Key="ListViewItemForegroundDisabled" ResourceKey="OnSurfaceDisabledBrush" />
		</ResourceDictionary>

	</ResourceDictionary.ThemeDictionaries>

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- ListViewItem Style -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<Style x:Key="SimpleListViewItemStyle"
		   TargetType="ListViewItem">

		<Setter Property="FontFamily" Value="{ThemeResource BodyLargeFontFamily}" />
		<Setter Property="FontSize" Value="{ThemeResource BodyLargeFontSize}" />
		<Setter Property="Background" Value="{ThemeResource ListViewItemBackground}" />
		<Setter Property="Foreground" Value="{ThemeResource ListViewItemForeground}" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="MinHeight" Value="{ThemeResource SimpleIconLarge}" />
		<Setter Property="Padding" Value="{ThemeResource SimpleSpace200Thickness}" />
		<Setter Property="CornerRadius" Value="{ThemeResource SimpleRadius200CornerRadius}" />
		<Setter Property="TabNavigation" Value="Local" />
		<Setter Property="IsHoldingEnabled" Value="True" />
		<Setter Property="AllowDrop" Value="False" />
		<Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
		<Setter Property="FocusVisualMargin" Value="0" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListViewItem">
					<Grid x:Name="ContentBorder"
						  Background="{TemplateBinding Background}"
						  BorderBrush="{TemplateBinding BorderBrush}"
						  BorderThickness="{TemplateBinding BorderThickness}"
						  CornerRadius="{TemplateBinding CornerRadius}">

						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />

								<VisualState x:Name="PointerOver">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource ListViewItemBackgroundPointerOver}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundPointerOver}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Pressed">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource ListViewItemBackgroundPressed}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundPressed}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Selected">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource ListViewItemBackgroundSelected}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundSelected}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="PointerOverSelected">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource ListViewItemBackgroundPointerOverSelected}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundPointerOverSelected}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="PressedSelected">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource ListViewItemBackgroundPressedSelected}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundPressedSelected}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<VisualStateGroup x:Name="DisabledStates">
								<VisualState x:Name="Enabled" />
								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundDisabled}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<ContentPresenter x:Name="ContentPresenter"
										  ContentTransitions="{TemplateBinding ContentTransitions}"
										  ContentTemplate="{TemplateBinding ContentTemplate}"
										  Content="{TemplateBinding Content}"
										  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
										  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
										  Margin="{TemplateBinding Padding}" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- ListView Style -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<Style x:Key="SimpleListViewStyle"
		   TargetType="ListView">

		<Setter Property="Background" Value="{ThemeResource ListViewBackground}" />
		<Setter Property="BorderBrush" Value="{ThemeResource ListViewBorderBrush}" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="IsTabStop" Value="False" />
		<Setter Property="TabNavigation" Value="Once" />
		<Setter Property="IsSwipeEnabled" Value="True" />
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
		<Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled" />
		<Setter Property="ScrollViewer.IsHorizontalRailEnabled" Value="False" />
		<Setter Property="ScrollViewer.VerticalScrollMode" Value="Enabled" />
		<Setter Property="ScrollViewer.IsVerticalRailEnabled" Value="True" />
		<Setter Property="ScrollViewer.ZoomMode" Value="Disabled" />
		<Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="False" />
		<Setter Property="ScrollViewer.BringIntoViewOnFocusChange" Value="True" />
		<Setter Property="ItemContainerStyle" Value="{StaticResource SimpleListViewItemStyle}" />

		<Setter Property="ItemContainerTransitions">
			<Setter.Value>
				<TransitionCollection>
					<AddDeleteThemeTransition />
					<ContentThemeTransition />
					<ReorderThemeTransition />
					<EntranceThemeTransition IsStaggeringEnabled="False" />
				</TransitionCollection>
			</Setter.Value>
		</Setter>

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListView">
					<Border Background="{TemplateBinding Background}"
							BorderBrush="{TemplateBinding BorderBrush}"
							BorderThickness="{TemplateBinding BorderThickness}"
							CornerRadius="{TemplateBinding CornerRadius}">

						<ScrollViewer x:Name="ScrollViewer"
									  TabNavigation="{TemplateBinding TabNavigation}"
									  HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
									  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
									  IsHorizontalScrollChainingEnabled="{TemplateBinding ScrollViewer.IsHorizontalScrollChainingEnabled}"
									  VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
									  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
									  IsVerticalScrollChainingEnabled="{TemplateBinding ScrollViewer.IsVerticalScrollChainingEnabled}"
									  IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}"
									  IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}"
									  ZoomMode="{TemplateBinding ScrollViewer.ZoomMode}"
									  IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
									  BringIntoViewOnFocusChange="{TemplateBinding ScrollViewer.BringIntoViewOnFocusChange}"
									  AutomationProperties.AccessibilityView="Raw">

							<ItemsPresenter Header="{TemplateBinding Header}"
											HeaderTemplate="{TemplateBinding HeaderTemplate}"
											HeaderTransitions="{TemplateBinding HeaderTransitions}"
											Footer="{TemplateBinding Footer}"
											FooterTemplate="{TemplateBinding FooterTemplate}"
											FooterTransitions="{TemplateBinding FooterTransitions}"
											Padding="{TemplateBinding Padding}" />
						</ScrollViewer>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- Default aliases -->
	<Style x:Key="SimpleDefaultListViewItemStyle"
		   TargetType="ListViewItem"
		   BasedOn="{StaticResource SimpleListViewItemStyle}" />

	<Style x:Key="SimpleDefaultListViewStyle"
		   TargetType="ListView"
		   BasedOn="{StaticResource SimpleListViewStyle}" />

</ResourceDictionary>
