<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					mc:Ignorable="">

	<!--
		Simple Design System — ListView / ListViewItem Style
		Maps SDS ListBox component tokens to WinUI ListView + ListViewItem.

		SDS ListBox:
		  Container: bg = background-default-default, border = border-default-default, radius = 8px
		  Item normal: transparent bg, text = text-default-default, description = text-default-secondary
		  Item focused/selected: bg = background-brand-default, text = text-brand-on-brand
		  Item disabled: text = text-disabled-default
	-->

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- Lightweight styling ThemeResource aliases                              -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<ResourceDictionary.ThemeDictionaries>

		<!-- ─── Light Theme ─── -->
		<ResourceDictionary x:Key="Light">
			<!-- ListView container -->
			<StaticResource x:Key="SimpleListViewBackground" ResourceKey="SimpleBackgroundDefaultDefaultBrush" />
			<StaticResource x:Key="SimpleListViewBorderBrush" ResourceKey="SimpleBorderDefaultDefaultBrush" />

			<!-- ListViewItem — Normal -->
			<SolidColorBrush x:Key="SimpleListViewItemBackground" Color="Transparent" />
			<StaticResource x:Key="SimpleListViewItemForeground" ResourceKey="SimpleTextDefaultDefaultBrush" />
			<StaticResource x:Key="SimpleListViewItemSecondaryForeground" ResourceKey="SimpleTextDefaultSecondaryBrush" />

			<!-- ListViewItem — PointerOver -->
			<StaticResource x:Key="SimpleListViewItemBackgroundPointerOver" ResourceKey="SimpleBackgroundDefaultSecondaryBrush" />
			<StaticResource x:Key="SimpleListViewItemForegroundPointerOver" ResourceKey="SimpleTextDefaultDefaultBrush" />

			<!-- ListViewItem — Pressed -->
			<StaticResource x:Key="SimpleListViewItemBackgroundPressed" ResourceKey="SimpleBackgroundDefaultTertiaryBrush" />
			<StaticResource x:Key="SimpleListViewItemForegroundPressed" ResourceKey="SimpleTextDefaultDefaultBrush" />

			<!-- ListViewItem — Selected (maps to SDS focused/selected highlight) -->
			<StaticResource x:Key="SimpleListViewItemBackgroundSelected" ResourceKey="SimpleBackgroundBrandDefaultBrush" />
			<StaticResource x:Key="SimpleListViewItemForegroundSelected" ResourceKey="SimpleTextBrandOnBrandBrush" />

			<!-- ListViewItem — PointerOverSelected -->
			<StaticResource x:Key="SimpleListViewItemBackgroundSelectedPointerOver" ResourceKey="SimpleBackgroundBrandHoverBrush" />
			<StaticResource x:Key="SimpleListViewItemForegroundSelectedPointerOver" ResourceKey="SimpleTextBrandOnBrandBrush" />

			<!-- ListViewItem — PressedSelected -->
			<StaticResource x:Key="SimpleListViewItemBackgroundSelectedPressed" ResourceKey="SimpleBackgroundBrandHoverBrush" />
			<StaticResource x:Key="SimpleListViewItemForegroundSelectedPressed" ResourceKey="SimpleTextBrandOnBrandBrush" />

			<!-- ListViewItem — Disabled -->
			<StaticResource x:Key="SimpleListViewItemForegroundDisabled" ResourceKey="SimpleTextDisabledDefaultBrush" />
		</ResourceDictionary>

		<!-- ─── Dark Theme (Default) ─── -->
		<ResourceDictionary x:Key="Default">
			<StaticResource x:Key="SimpleListViewBackground" ResourceKey="SimpleBackgroundDefaultDefaultBrush" />
			<StaticResource x:Key="SimpleListViewBorderBrush" ResourceKey="SimpleBorderDefaultDefaultBrush" />

			<SolidColorBrush x:Key="SimpleListViewItemBackground" Color="Transparent" />
			<StaticResource x:Key="SimpleListViewItemForeground" ResourceKey="SimpleTextDefaultDefaultBrush" />
			<StaticResource x:Key="SimpleListViewItemSecondaryForeground" ResourceKey="SimpleTextDefaultSecondaryBrush" />

			<StaticResource x:Key="SimpleListViewItemBackgroundPointerOver" ResourceKey="SimpleBackgroundDefaultSecondaryBrush" />
			<StaticResource x:Key="SimpleListViewItemForegroundPointerOver" ResourceKey="SimpleTextDefaultDefaultBrush" />

			<StaticResource x:Key="SimpleListViewItemBackgroundPressed" ResourceKey="SimpleBackgroundDefaultTertiaryBrush" />
			<StaticResource x:Key="SimpleListViewItemForegroundPressed" ResourceKey="SimpleTextDefaultDefaultBrush" />

			<StaticResource x:Key="SimpleListViewItemBackgroundSelected" ResourceKey="SimpleBackgroundBrandDefaultBrush" />
			<StaticResource x:Key="SimpleListViewItemForegroundSelected" ResourceKey="SimpleTextBrandOnBrandBrush" />

			<StaticResource x:Key="SimpleListViewItemBackgroundSelectedPointerOver" ResourceKey="SimpleBackgroundBrandHoverBrush" />
			<StaticResource x:Key="SimpleListViewItemForegroundSelectedPointerOver" ResourceKey="SimpleTextBrandOnBrandBrush" />

			<StaticResource x:Key="SimpleListViewItemBackgroundSelectedPressed" ResourceKey="SimpleBackgroundBrandHoverBrush" />
			<StaticResource x:Key="SimpleListViewItemForegroundSelectedPressed" ResourceKey="SimpleTextBrandOnBrandBrush" />

			<StaticResource x:Key="SimpleListViewItemForegroundDisabled" ResourceKey="SimpleTextDisabledDefaultBrush" />
		</ResourceDictionary>

	</ResourceDictionary.ThemeDictionaries>

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- ListViewItem Style                                                     -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<Style x:Key="SimpleListViewItemStyle"
		   TargetType="ListViewItem">

		<Setter Property="FontFamily" Value="{ThemeResource SimpleFontFamily}" />
		<Setter Property="FontSize" Value="16" />
		<Setter Property="Background" Value="{ThemeResource SimpleListViewItemBackground}" />
		<Setter Property="Foreground" Value="{ThemeResource SimpleListViewItemForeground}" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="MinHeight" Value="40" />
		<Setter Property="Padding" Value="8" />
		<Setter Property="CornerRadius" Value="8" />
		<Setter Property="TabNavigation" Value="Local" />
		<Setter Property="IsHoldingEnabled" Value="True" />
		<Setter Property="AllowDrop" Value="False" />
		<Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
		<Setter Property="FocusVisualMargin" Value="0" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListViewItem">
					<Grid x:Name="ContentBorder"
						  Background="{TemplateBinding Background}"
						  BorderBrush="{TemplateBinding BorderBrush}"
						  BorderThickness="{TemplateBinding BorderThickness}"
						  CornerRadius="{TemplateBinding CornerRadius}">

						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />

								<VisualState x:Name="PointerOver">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource SimpleListViewItemBackgroundPointerOver}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SimpleListViewItemForegroundPointerOver}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Pressed">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource SimpleListViewItemBackgroundPressed}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SimpleListViewItemForegroundPressed}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Selected">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource SimpleListViewItemBackgroundSelected}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SimpleListViewItemForegroundSelected}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="PointerOverSelected">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource SimpleListViewItemBackgroundSelectedPointerOver}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SimpleListViewItemForegroundSelectedPointerOver}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="PressedSelected">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource SimpleListViewItemBackgroundSelectedPressed}" />
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SimpleListViewItemForegroundSelectedPressed}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<VisualStateGroup x:Name="DisabledStates">
								<VisualState x:Name="Enabled" />
								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SimpleListViewItemForegroundDisabled}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<ContentPresenter x:Name="ContentPresenter"
										  ContentTransitions="{TemplateBinding ContentTransitions}"
										  ContentTemplate="{TemplateBinding ContentTemplate}"
										  Content="{TemplateBinding Content}"
										  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
										  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
										  Margin="{TemplateBinding Padding}" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<!-- ListView Style                                                        -->
	<!-- ═══════════════════════════════════════════════════════════════════════ -->
	<Style x:Key="SimpleListViewStyle"
		   TargetType="ListView">

		<Setter Property="Background" Value="{ThemeResource SimpleListViewBackground}" />
		<Setter Property="BorderBrush" Value="{ThemeResource SimpleListViewBorderBrush}" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="IsTabStop" Value="False" />
		<Setter Property="TabNavigation" Value="Once" />
		<Setter Property="IsSwipeEnabled" Value="True" />
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
		<Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled" />
		<Setter Property="ScrollViewer.IsHorizontalRailEnabled" Value="False" />
		<Setter Property="ScrollViewer.VerticalScrollMode" Value="Enabled" />
		<Setter Property="ScrollViewer.IsVerticalRailEnabled" Value="True" />
		<Setter Property="ScrollViewer.ZoomMode" Value="Disabled" />
		<Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="False" />
		<Setter Property="ScrollViewer.BringIntoViewOnFocusChange" Value="True" />
		<Setter Property="ItemContainerStyle" Value="{StaticResource SimpleListViewItemStyle}" />

		<Setter Property="ItemContainerTransitions">
			<Setter.Value>
				<TransitionCollection>
					<AddDeleteThemeTransition />
					<ContentThemeTransition />
					<ReorderThemeTransition />
					<EntranceThemeTransition IsStaggeringEnabled="False" />
				</TransitionCollection>
			</Setter.Value>
		</Setter>

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListView">
					<Border Background="{TemplateBinding Background}"
							BorderBrush="{TemplateBinding BorderBrush}"
							BorderThickness="{TemplateBinding BorderThickness}"
							CornerRadius="{TemplateBinding CornerRadius}">

						<ScrollViewer x:Name="ScrollViewer"
									  TabNavigation="{TemplateBinding TabNavigation}"
									  HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
									  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
									  IsHorizontalScrollChainingEnabled="{TemplateBinding ScrollViewer.IsHorizontalScrollChainingEnabled}"
									  VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
									  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
									  IsVerticalScrollChainingEnabled="{TemplateBinding ScrollViewer.IsVerticalScrollChainingEnabled}"
									  IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}"
									  IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}"
									  ZoomMode="{TemplateBinding ScrollViewer.ZoomMode}"
									  IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
									  BringIntoViewOnFocusChange="{TemplateBinding ScrollViewer.BringIntoViewOnFocusChange}"
									  AutomationProperties.AccessibilityView="Raw">

							<ItemsPresenter Header="{TemplateBinding Header}"
											HeaderTemplate="{TemplateBinding HeaderTemplate}"
											HeaderTransitions="{TemplateBinding HeaderTransitions}"
											Footer="{TemplateBinding Footer}"
											FooterTemplate="{TemplateBinding FooterTemplate}"
											FooterTransitions="{TemplateBinding FooterTransitions}"
											Padding="{TemplateBinding Padding}" />
						</ScrollViewer>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- Default aliases -->
	<Style x:Key="SimpleDefaultListViewItemStyle"
		   TargetType="ListViewItem"
		   BasedOn="{StaticResource SimpleListViewItemStyle}" />

	<Style x:Key="SimpleDefaultListViewStyle"
		   TargetType="ListView"
		   BasedOn="{StaticResource SimpleListViewStyle}" />

</ResourceDictionary>
