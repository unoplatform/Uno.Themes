<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
					mc:Ignorable="d">

	<x:Double x:Key="ProgressBarMinWidth">250</x:Double>
	<x:Double x:Key="ProgressBarHeight">4</x:Double>

	<ResourceDictionary.ThemeDictionaries>

		<ResourceDictionary x:Key="Default">

			<StaticResource x:Key="MaterialProgressBarForeground" ResourceKey="PrimaryBrush" />

			<StaticResource x:Key="MaterialProgressBarBackground" ResourceKey="SurfaceVariantBrush" />

			<StaticResource x:Key="MaterialProgressBarMinWidth" ResourceKey="ProgressBarMinWidth" />

			<StaticResource x:Key="MaterialProgressBarHeight" ResourceKey="ProgressBarHeight" />
		</ResourceDictionary>

		<ResourceDictionary x:Key="Light">

			<StaticResource x:Key="MaterialProgressBarForeground" ResourceKey="PrimaryBrush" />

			<StaticResource x:Key="MaterialProgressBarBackground" ResourceKey="SurfaceVariantBrush" />

			<StaticResource x:Key="MaterialProgressBarMinWidth" ResourceKey="ProgressBarMinWidth" />

			<StaticResource x:Key="MaterialProgressBarHeight" ResourceKey="ProgressBarHeight" />
		</ResourceDictionary>
	</ResourceDictionary.ThemeDictionaries>

	<Style x:Key="MaterialProgressBarStyle"
		   TargetType="muxc:ProgressBar">
		<Setter Property="Foreground"
				Value="{ThemeResource MaterialProgressBarForeground}" />
		<Setter Property="Background"
				Value="{ThemeResource MaterialProgressBarBackground}" />
		<Setter Property="MinWidth"
				Value="{ThemeResource MaterialProgressBarMinWidth}" />
		<Setter Property="Height"
				Value="{ThemeResource MaterialProgressBarHeight}" />
	</Style>

</ResourceDictionary>
