<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:not_win="http://uno.ui/not_win"
					xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
					mc:Ignorable="d not_win">

	<!-- PipsPager Resources -->
	
	<x:Double x:Key="MaterialPipsPagerButtonWidth">20</x:Double>
	<x:Double x:Key="MaterialPipsPagerButtonHeight">20</x:Double>

	<x:String x:Key="MaterialPipsPagerSelectedGlyph">&#xEA3B;</x:String>
	<x:String x:Key="MaterialPipsPagerDefaultGlyph">&#xEA3B;</x:String>

	<x:String x:Key="MaterialPipsPagerSelectedGlyphFontSize">8</x:String>
	<x:String x:Key="MaterialPipsPagerDefaultGlyphFontSize">8</x:String>

	<Style x:Key="MaterialPipsPagerButtonBaseStyle"
		   TargetType="Button">
		<Setter Property="Background"
				Value="Transparent" />
		<Setter Property="FontFamily"
				Value="{StaticResource SymbolThemeFontFamily}" />
		<Setter Property="HorizontalContentAlignment"
				Value="Center" />
		<Setter Property="VerticalContentAlignment"
				Value="Center" />
		<Setter Property="UseSystemFocusVisuals"
				Value="{StaticResource UseSystemFocusVisuals}" />
		<Setter Property="HorizontalContentAlignment"
				Value="Center" />
		<Setter Property="Width"
				Value="{StaticResource MaterialPipsPagerButtonWidth}" />
		<Setter Property="Height"
				Value="{StaticResource MaterialPipsPagerButtonHeight}" />
		<Setter Property="Content"
				Value="{StaticResource MaterialPipsPagerDefaultGlyph}" />
		<Setter Property="FontSize"
				Value="{StaticResource MaterialPipsPagerDefaultGlyphFontSize}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="Button">
					<Grid x:Name="RootGrid"
						  Background="{TemplateBinding Background}"
						  BorderBrush="{TemplateBinding BorderBrush}"
						  BorderThickness="{TemplateBinding BorderThickness}">
						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />
								<VisualState x:Name="Pressed" />
								<VisualState x:Name="PointerOver" />
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>
						<FontIcon x:Name="Content"
								  FontSize="{TemplateBinding FontSize}"
								  FontFamily="{TemplateBinding FontFamily}"
								  Glyph="{TemplateBinding Content}"
								  MirroredWhenRightToLeft="True"
								  Foreground="{TemplateBinding Foreground}"
								  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
								  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
								  AutomationProperties.AccessibilityView="Raw" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="MaterialPipsPagerSelectedPipButtonStyle"
		   BasedOn="{StaticResource MaterialPipsPagerButtonBaseStyle}"
		   TargetType="Button">
		<Setter Property="Content"
				Value="{StaticResource MaterialPipsPagerSelectedGlyph}" />
		<Setter Property="FontSize"
				Value="{StaticResource MaterialPipsPagerSelectedGlyphFontSize}" />
		<Setter Property="Foreground"
				Value="{ThemeResource PrimaryBrush}" />
	</Style>

	<Style x:Key="MaterialPipsPagerDefaultPipButtonStyle"
		   BasedOn="{StaticResource MaterialPipsPagerButtonBaseStyle}"
		   TargetType="Button">
		<Setter Property="Content"
				Value="{StaticResource MaterialPipsPagerDefaultGlyph}" />
		<Setter Property="FontSize"
				Value="{StaticResource MaterialPipsPagerDefaultGlyphFontSize}" />
		<Setter Property="Foreground"
				Value="{ThemeResource SurfaceVariantBrush}" />
	</Style>

	<Style x:Key="MaterialPipsPagerStyle"
		   TargetType="muxc:PipsPager">
		<Setter Property="Background"
				Value="Transparent" />
		<Setter Property="HorizontalAlignment"
				Value="Left" />
		<Setter Property="VerticalAlignment"
				Value="Top" />
		<Setter Property="AutomationProperties.LandmarkType"
				Value="Navigation" />
		<!-- Uno already implemented the 2.8.0 MUX version of the control -->
		<!-- Uno.Themes reference is still on the MUX v2.6.0-prerelease.210113001 version for now -->
		<!-- Replace by <Setter Property=NormalPipStyle /> and <Setter Property=SelectedPipStyle /> only, once we can bump the Microsoft.UI.Xaml version -->
		<not_win:Setter Property="SelectedPipStyle"
					Value="{StaticResource MaterialPipsPagerSelectedPipButtonStyle}" />
		<not_win:Setter Property="NormalPipStyle"
					Value="{StaticResource MaterialPipsPagerDefaultPipButtonStyle}" />
		<win:Setter Property="SelectedIndicatorButtonStyle"
				Value="{StaticResource MaterialPipsPagerSelectedPipButtonStyle}" />
		<win:Setter Property="DefaultIndicatorButtonStyle"
				Value="{StaticResource MaterialPipsPagerDefaultPipButtonStyle}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="muxc:PipsPager">
					<StackPanel x:Name="RootPanel"
								Orientation="{TemplateBinding Orientation}"
								Background="{TemplateBinding Background}">
						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="PreviousPageButtonVisibilityStates">
								<VisualState x:Name="PreviousPageButtonVisible" />
								<VisualState x:Name="PreviousPageButtonHidden">
									<VisualState.Setters>
										<Setter Target="PreviousPageButton.Opacity"
												Value="0" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="PreviousPageButtonCollapsed">
									<VisualState.Setters>
										<Setter Target="PreviousPageButton.Visibility"
												Value="Collapsed" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<VisualStateGroup x:Name="PreviousPageButtonIsEnabledStates">
								<VisualState x:Name="PreviousPageButtonEnabled" />
								<VisualState x:Name="PreviousPageButtonDisabled">
									<VisualState.Setters>
										<Setter Target="PreviousPageButton.IsEnabled"
												Value="False" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<VisualStateGroup x:Name="NextPageButtonVisibilityStates">
								<VisualState x:Name="NextPageButtonVisible" />
								<VisualState x:Name="NextPageButtonHidden">
									<VisualState.Setters>
										<Setter Target="NextPageButton.Opacity"
												Value="0" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="NextPageButtonCollapsed">
									<VisualState.Setters>
										<Setter Target="NextPageButton.Visibility"
												Value="Collapsed" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<VisualStateGroup x:Name="NextPageButtonIsEnabledStates">
								<VisualState x:Name="NextPageButtonEnabled" />
								<VisualState x:Name="NextPageButtonDisabled">
									<VisualState.Setters>
										<Setter Target="NextPageButton.IsEnabled"
												Value="False" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<VisualStateGroup x:Name="RootPanelOrientationStates">
								<VisualState x:Name="VerticalOrientationView" />
								<VisualState x:Name="HorizontalOrientationView">
									<VisualState.Setters>
										<Setter Target="RootPanel.Orientation"
												Value="Horizontal" />
										<Setter Target="PreviousPageButton.RenderTransformOrigin"
												Value="0.5, 0.5" />
										<Setter Target="PreviousPageButton.RenderTransform">
											<Setter.Value>
												<RotateTransform Angle="-90" />
											</Setter.Value>
										</Setter>
										<Setter Target="NextPageButton.RenderTransformOrigin"
												Value="0.5, 0.5" />
										<Setter Target="NextPageButton.RenderTransform">
											<Setter.Value>
												<RotateTransform Angle="-90" />
											</Setter.Value>
										</Setter>
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<Button x:Name="PreviousPageButton"
								ToolTipService.ToolTip="{Binding ElementName=PreviousPageButton, Path=(AutomationProperties.Name)}"
								Style="{TemplateBinding PreviousButtonStyle}" />

						<ScrollViewer x:Name="PipsPagerScrollViewer"
									  VerticalScrollBarVisibility="Hidden"
									  VerticalScrollMode="Disabled"
									  HorizontalScrollBarVisibility="Hidden"
									  HorizontalScrollMode="Disabled">
							<muxc:ItemsRepeater x:Name="PipsPagerItemsRepeater"
												ItemsSource="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TemplateSettings.PipsPagerItems}">
								<muxc:ItemsRepeater.Layout>
									<muxc:StackLayout Orientation="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Orientation}" />
								</muxc:ItemsRepeater.Layout>
								<muxc:ItemsRepeater.ItemTemplate>
									<DataTemplate>
										<Button />
									</DataTemplate>
								</muxc:ItemsRepeater.ItemTemplate>
							</muxc:ItemsRepeater>
						</ScrollViewer>

						<Button x:Name="NextPageButton"
								ToolTipService.ToolTip="{Binding ElementName=NextPageButton, Path=(AutomationProperties.Name)}"
								Style="{TemplateBinding NextButtonStyle}" />
					</StackPanel>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
