<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<Style x:Key="MaterialBaseTextBlockStyle"
		   TargetType="TextBlock">

		<Setter Property="TextTrimming"
				Value="CharacterEllipsis" />
		<Setter Property="TextWrapping"
				Value="Wrap" />
		<Setter Property="FontWeight"
				Value="Normal" />
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialRegularFontFamily}" />
	</Style>

	<!-- IMPORTANT NOTE :
	CharacterSpacing is measured in 1/1000 of an "em". One "em" is equivalent to the current font size of the control.
	For example, here with MaterialLabelMedium an additional 12 pixels will be inserted between each character with FontSize="12" and CharacterSpacing="1000".
	So in order to specify the letter spacing of 0.5px from the M3 Material guidelines, we will need to add CharacterSpacing="41.666".
	To resume: CharacterSpacing = (letter spacing or tracking value / FontSize) * 1000 -->

	<!-- DISPLAY -->

	<!--  Display Large  -->
	<Style x:Key="MaterialDisplayLarge"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialDisplayLargeFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialDisplayLargeFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialDisplayLargeFontSize}" />
		<Setter Property="CharacterSpacing"
				Value="{ThemeResource MaterialDisplayLargeCharacterSpacing}" />
	</Style>

	<!--  Display Medium  -->
	<Style x:Key="MaterialDisplayMedium"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialDisplayMediumFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialDisplayMediumFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialDisplayMediumFontSize}" />
	</Style>

	<!--  Display Small  -->
	<Style x:Key="MaterialDisplaySmall"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialDisplaySmallFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialDisplaySmallFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialDisplaySmallFontSize}" />
	</Style>

	<!-- HEADLINE -->

	<!--  Headline Large  -->
	<Style x:Key="MaterialHeadlineLarge"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialHeadlineLargeFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialHeadlineLargeFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialHeadlineLargeFontSize}" />
	</Style>

	<!--  Headline Medium  -->
	<Style x:Key="MaterialHeadlineMedium"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialHeadlineMediumFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialHeadlineMediumFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialHeadlineMediumFontSize}" />
	</Style>

	<!--  Headline Small  -->
	<Style x:Key="MaterialHeadlineSmall"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialHeadlineSmallFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialHeadlineSmallFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialHeadlineSmallFontSize}" />
	</Style>

	<!-- TITLE -->

	<!--  Title Large  -->
	<Style x:Key="MaterialTitleLarge"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialTitleLargeFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialTitleLargeFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialTitleLargeFontSize}" />
	</Style>

	<!--  Title Medium  -->
	<Style x:Key="MaterialTitleMedium"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialTitleMediumFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialTitleMediumFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialTitleMediumFontSize}" />
	</Style>

	<!--  Title Small  -->
	<Style x:Key="MaterialTitleSmall"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialTitleSmallFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialTitleSmallFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialTitleSmallFontSize}" />
	</Style>

	<!-- LABEL -->

	<!--  Label Large  -->
	<Style x:Key="MaterialLabelLarge"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialLabelLargeFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialLabelLargeFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialLabelLargeFontSize}" />
		<Setter Property="CharacterSpacing"
				Value="{ThemeResource MaterialLabelLargeCharacterSpacing}" />
	</Style>

	<!--  Label Medium  -->
	<Style x:Key="MaterialLabelMedium"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialLabelMediumFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialLabelMediumFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialLabelMediumFontSize}" />
		<Setter Property="CharacterSpacing"
				Value="{ThemeResource MaterialLabelMediumCharacterSpacing}" />
	</Style>

	<!--  Label Small  -->
	<Style x:Key="MaterialLabelSmall"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialLabelSmallFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialLabelSmallFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialLabelSmallFontSize}" />
		<Setter Property="CharacterSpacing"
				Value="{ThemeResource MaterialLabelSmallCharacterSpacing}" />
	</Style>

	<!--  Label ExtraSmall  -->
	<!-- Text style added for Badges so it will match Uno figma file -->
	<!-- Not part of Material M3. See https://github.com/unoplatform/Uno.Figma/issues/628 -->
	<Style x:Key="MaterialLabelExtraSmall"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialLabelExtraSmallFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialLabelExtraSmallFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialLabelExtraSmallFontSize}" />
		<Setter Property="CharacterSpacing"
				Value="{ThemeResource MaterialLabelExtraSmallCharacterSpacing}" />
	</Style>

	<!-- BODY -->

	<!--  Body Large  -->
	<Style x:Key="MaterialBodyLarge"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialBodyLargeFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialBodyLargeFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialBodyLargeFontSize}" />
		<Setter Property="CharacterSpacing"
				Value="{ThemeResource MaterialBodyLargeCharacterSpacing}" />
	</Style>

	<!--  Body Medium  -->
	<Style x:Key="MaterialBodyMedium"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource BodyMediumFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialBodyMediumFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialBodyMediumFontSize}" />
		<Setter Property="CharacterSpacing"
				Value="{ThemeResource MaterialBodyMediumCharacterSpacing}" />
	</Style>

	<!--  Body Small  -->
	<Style x:Key="MaterialBodySmall"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialBodySmallFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialBodySmallFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialBodySmallFontSize}" />
		<Setter Property="CharacterSpacing"
				Value="{ThemeResource MaterialBodySmallCharacterSpacing}" />
	</Style>

	<!-- CAPTION -->

	<!--  Caption Large  -->
	<!-- Gap Filler for the other design system - Not part of Material M3 -->
	<Style x:Key="MaterialCaptionLarge"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialCaptionLargeFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialCaptionLargeFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialCaptionLargeFontSize}" />
		<Setter Property="CharacterSpacing"
				Value="{ThemeResource MaterialCaptionLargeCharacterSpacing}" />
	</Style>

	<!--  Caption Medium  -->
	<!-- Gap Filler for the other design system - Not part of Material M3 -->
	<Style x:Key="MaterialCaptionMedium"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialCaptionMediumFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialCaptionMediumFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialCaptionMediumFontSize}" />
		<Setter Property="CharacterSpacing"
				Value="{ThemeResource MaterialCaptionMediumCharacterSpacing}" />
	</Style>

	<!--  Caption Small  -->
	<!-- Gap Filler for the other design system - Not part of Material M3 -->
	<Style x:Key="MaterialCaptionSmall"
		   BasedOn="{StaticResource MaterialBaseTextBlockStyle}"
		   TargetType="TextBlock">
		<Setter Property="FontFamily"
				Value="{ThemeResource MaterialCaptionSmallFontFamily}" />
		<Setter Property="FontWeight"
				Value="{ThemeResource MaterialCaptionSmallFontWeight}" />
		<Setter Property="FontSize"
				Value="{ThemeResource MaterialCaptionSmallFontSize}" />
		<Setter Property="CharacterSpacing"
				Value="{ThemeResource MaterialCaptionSmallCharacterSpacing}" />
	</Style>

</ResourceDictionary>
