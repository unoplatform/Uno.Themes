<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:um="using:Uno.Material"
					xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
					mc:Ignorable="d">

	<!-- Implicit styles -->
	<Style BasedOn="{StaticResource MaterialDefaultAppBarButtonStyle}"
		   TargetType="AppBarButton" />
	<Style BasedOn="{StaticResource MaterialDefaultButtonStyle}"
		   TargetType="Button" />
	<Style BasedOn="{StaticResource MaterialDefaultCalendarDatePickerStyle}"
		   TargetType="CalendarDatePicker" />
	<Style BasedOn="{StaticResource MaterialDefaultCalendarViewStyle}"
		   TargetType="CalendarView" />
	<Style BasedOn="{StaticResource MaterialDefaultCheckBoxStyle}"
		   TargetType="CheckBox" />
	<Style BasedOn="{StaticResource MaterialDefaultComboBoxStyle}"
		   TargetType="ComboBox" />
	<Style BasedOn="{StaticResource MaterialDefaultCommandBarStyle}"
		   TargetType="CommandBar" />
	<Style BasedOn="{StaticResource MaterialDefaultContentDialogStyle}"
		   TargetType="ContentDialog" />
	<Style BasedOn="{StaticResource MaterialDefaultDatePickerStyle}"
		   TargetType="DatePicker" />
	<Style BasedOn="{StaticResource MaterialDefaultDatePickerFlyoutPresenterStyle}"
		   TargetType="DatePickerFlyoutPresenter" />
	<Style BasedOn="{StaticResource MaterialDefaultFlyoutPresenterStyle}"
		   TargetType="FlyoutPresenter" />
	<Style BasedOn="{StaticResource MaterialDefaultHyperlinkButtonStyle}"
		   TargetType="HyperlinkButton" />
	<Style BasedOn="{StaticResource MaterialDefaultListViewItemStyle}"
		   TargetType="ListViewItem" />
	<Style BasedOn="{StaticResource MaterialDefaultListViewStyle}"
		   TargetType="ListView" />
	<Style BasedOn="{StaticResource MaterialDefaultMediaTransportControlsStyle}"
		   TargetType="MediaTransportControls" />
	<Style BasedOn="{StaticResource MaterialDefaultMenuFlyoutItemStyle}"
		   TargetType="MenuFlyoutItem" />
	<Style BasedOn="{StaticResource MaterialDefaultMenuFlyoutPresenterStyle}"
		   TargetType="MenuFlyoutPresenter" />
	<Style BasedOn="{StaticResource MaterialDefaultMenuFlyoutSeparatorStyle}"
		   TargetType="MenuFlyoutSeparator" />
	<Style BasedOn="{StaticResource MaterialDefaultMenuFlyoutSubItemStyle}"
		   TargetType="MenuFlyoutSubItem" />
	<Style BasedOn="{StaticResource MaterialDefaultNavigationViewStyle}"
		   TargetType="muxc:NavigationView" />
	<Style BasedOn="{StaticResource MaterialDefaultNavigationViewItemStyle}"
		   TargetType="muxc:NavigationViewItem" />
	<Style BasedOn="{StaticResource MaterialDefaultPasswordBoxStyle}"
		   TargetType="PasswordBox" />
	<Style BasedOn="{StaticResource MaterialDefaultPipsPagerStyle}"
		   TargetType="muxc:PipsPager" />
	<Style BasedOn="{StaticResource MaterialDefaultProgressBarStyle}"
		   TargetType="muxc:ProgressBar" />
	<Style BasedOn="{StaticResource MaterialDefaultProgressRingStyle}"
		   TargetType="muxc:ProgressRing" />
	<Style BasedOn="{StaticResource MaterialDefaultRadioButtonStyle}"
		   TargetType="RadioButton" />
	<Style BasedOn="{StaticResource MaterialDefaultRadioMenuFlyoutItemStyle}"
		   TargetType="muxc:RadioMenuFlyoutItem" />
	<Style BasedOn="{StaticResource MaterialDefaultRatingControlStyle}"
		   TargetType="RatingControl" />
	<Style BasedOn="{StaticResource MaterialDefaultRippleStyle}"
		   TargetType="um:Ripple" />
	<Style BasedOn="{StaticResource MaterialDefaultSliderStyle}"
		   TargetType="Slider" />
	<Style BasedOn="{StaticResource MaterialDefaultTextBlockStyle}"
		   TargetType="TextBlock" />
	<Style BasedOn="{StaticResource MaterialDefaultTextBoxStyle}"
		   TargetType="TextBox" />
	<Style BasedOn="{StaticResource MaterialDefaultToggleButtonStyle}"
		   TargetType="ToggleButton" />
	<Style BasedOn="{StaticResource MaterialDefaultToggleMenuFlyoutItemStyle}"
		   TargetType="ToggleMenuFlyoutItem" />
	<Style BasedOn="{StaticResource MaterialDefaultToggleSwitchStyle}"
		   TargetType="ToggleSwitch" />

	<!-- Legacy alias; Material-named, so not a semantic key -->
	<StaticResource x:Key="DefaultMaterialCalendarViewStyle" ResourceKey="MaterialDefaultCalendarViewStyle" />

</ResourceDictionary>
