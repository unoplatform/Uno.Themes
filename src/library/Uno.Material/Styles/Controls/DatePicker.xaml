<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:android="http://uno.ui/android"
					xmlns:ios="http://uno.ui/ios"
					xmlns:wasm="http://uno.ui/wasm"
					mc:Ignorable="ios android wasm">

	<!-- DatePicker Style -->
	<Style x:Key="DatePickerFlyoutButtonStyle"
		   TargetType="Button">

		<Setter Property="Background"
				Value="Transparent" />
		<Setter Property="Padding"
				Value="0" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="Button">

					<Grid x:Name="RootGrid"
						  Background="{TemplateBinding Background}">

						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />
								<VisualState x:Name="PointerOver" />
								<VisualState x:Name="Pressed">
									<VisualState.Setters>
										<Setter Target="RootGrid.Opacity"
												Value="0.65" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="RootGrid.Opacity"
												Value="0.65" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<!-- Label -->
						<ContentPresenter x:Name="ContentPresenter"
										  Content="{TemplateBinding Content}"
										  ContentTransitions="{TemplateBinding ContentTransitions}"
										  ContentTemplate="{TemplateBinding ContentTemplate}"
										  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
										  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
										  Margin="{TemplateBinding Padding}"
										  AutomationProperties.AccessibilityView="Raw" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="MaterialDatePickerStyle"
		   TargetType="DatePicker">

		<Setter Property="Background"
				Value="{StaticResource TextBoxFilledBackgroundColorBrush}" />
		<Setter Property="Foreground"
				Value="{StaticResource MaterialPrimaryBrush}" />
		<Setter Property="BorderBrush"
				Value="{StaticResource MaterialPrimaryBrush}" />
		<Setter Property="HorizontalAlignment"
				Value="Stretch" />
		<Setter Property="Height"
				Value="53" />
		<Setter Property="CornerRadius"
				Value="4,4,0,0" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="DatePicker">

					<!-- Root Grid -->
					<Grid x:Name="LayoutRoot">

						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />

								<VisualState x:Name="Disabled">
									<VisualState.Setters>

										<Setter Target="HeaderTextBlock.Foreground"
												Value="{StaticResource MaterialOnSurfaceLowBrush}" />
										
										<Setter Target="DayTextBlock.Foreground"
												Value="{StaticResource MaterialOnSurfaceLowBrush}" />
										<Setter Target="FirstPickerSpacing.Foreground"
												Value="{StaticResource MaterialOnSurfaceLowBrush}" />
										<Setter Target="MonthTextBlock.Foreground"
												Value="{StaticResource MaterialOnSurfaceLowBrush}" />
										<Setter Target="SecondPickerSpacing.Foreground"
												Value="{StaticResource MaterialOnSurfaceLowBrush}" />
										<Setter Target="YearTextBlock.Foreground"
												Value="{StaticResource MaterialOnSurfaceLowBrush}" />

										<Setter Target="BottomBorder.Fill"
												Value="{StaticResource MaterialOnSurfaceLowBrush}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<VisualStateGroup x:Name="HasDateStates">
								<VisualState x:Name="HasDate" />
								<VisualState x:Name="HasNoDate">
									<VisualState.Setters>
										<Setter Target="DayTextBlock.Text"
												Value="dd" />
										<Setter Target="DayTextBlock.Foreground"
												Value="{StaticResource MaterialOnSurfaceLowBrush}" />

										<Setter Target="FirstPickerSpacing.Foreground"
												Value="{StaticResource MaterialOnSurfaceLowBrush}" />

										<Setter Target="MonthTextBlock.Text"
												Value="mm" />
										<Setter Target="MonthTextBlock.Foreground"
												Value="{StaticResource MaterialOnSurfaceLowBrush}" />

										<Setter Target="SecondPickerSpacing.Foreground"
												Value="{StaticResource MaterialOnSurfaceLowBrush}" />

										<Setter Target="YearTextBlock.Text"
												Value="yyyy" />
										<Setter Target="YearTextBlock.Foreground"
												Value="{StaticResource MaterialOnSurfaceLowBrush}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<!-- Flyout Button -->
						<Button x:Name="FlyoutButton"
								Style="{StaticResource DatePickerFlyoutButtonStyle}"
								IsEnabled="{TemplateBinding IsEnabled}"
								UseSystemFocusVisuals="{TemplateBinding UseSystemFocusVisuals}"
								HorizontalAlignment="Stretch"
								HorizontalContentAlignment="Stretch">

							<Grid Background="{TemplateBinding Background}"
								  CornerRadius="{TemplateBinding CornerRadius}"
								  Height="{TemplateBinding Height}">

								<!-- Border -->
								<Rectangle x:Name="BottomBorder"
										   Fill="{TemplateBinding BorderBrush}"
										   VerticalAlignment="Bottom"
										   Height="2" />

								<!-- Header -->
								<TextBlock x:Name="HeaderTextBlock"
										   Text="{TemplateBinding Header}"
										   Style="{StaticResource MaterialCaption}"
										   Foreground="{TemplateBinding Foreground}"
										   HorizontalAlignment="Stretch"
										   VerticalAlignment="Top"
										   TextWrapping="Wrap"
										   Margin="10,8,10,0"
										   Grid.Row="0" />

								<Grid x:Name="FlyoutButtonContentGrid"
									  VerticalAlignment="Top"
									  Margin="10,24,10,0"
									  Height="24"
									  Grid.Row="1">

									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto"
														  x:Name="DayColumn" />
										<ColumnDefinition Width="Auto"
														  x:Name="FirstSpacerColumn" />
										<ColumnDefinition Width="Auto"
														  x:Name="MonthColumn" />
										<ColumnDefinition Width="Auto"
														  x:Name="SecondSpacerColumn" />
										<ColumnDefinition Width="Auto"
														  x:Name="YearColumn" />
									</Grid.ColumnDefinitions>

									<!-- Day -->
									<TextBlock x:Name="DayTextBlock"
											   Text="dd"
											   Style="{StaticResource MaterialBody2}"
											   AutomationProperties.AccessibilityView="Raw"
											   Padding="0"
											   Margin="1,0,0,0" />

									<!-- Month -->
									<TextBlock x:Name="MonthTextBlock"
											   Text="mm"
											   Style="{StaticResource MaterialBody2}"
											   AutomationProperties.AccessibilityView="Raw"
											   Padding="0"
											   Margin="1,0,0,0" />

									<!-- Year -->
									<TextBlock x:Name="YearTextBlock"
											   Text="yyyy"
											   Style="{StaticResource MaterialBody2}"
											   AutomationProperties.AccessibilityView="Raw"
											   Padding="0"
											   Margin="1,0,0,0" />

									<!-- / -->
									<TextBlock x:Name="FirstPickerSpacing"
											   Text="/"
											   Style="{StaticResource MaterialBody2}"
											   Padding="0"
											   Grid.Column="1" />

									<!-- / -->
									<TextBlock x:Name="SecondPickerSpacing"
											   Text="/"
											   Style="{StaticResource MaterialBody2}"
											   Padding="0"
											   Grid.Column="3" />
								</Grid>
							</Grid>
						</Button>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="MaterialSecondaryDatePickerStyle"
		   TargetType="DatePicker"
		   BasedOn="{StaticResource MaterialDatePickerStyle}">

		<Setter Property="Foreground"
				Value="{StaticResource MaterialSecondaryBrush}" />
		<Setter Property="BorderBrush"
				Value="{StaticResource MaterialSecondaryBrush}" />
	</Style>

</ResourceDictionary>
