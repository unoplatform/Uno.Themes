<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:uc="using:Uno.Cupertino">

	<!--
		A popover: content on Thick glass, behind a hairline, with the popover radius. Background and BorderBrush
		are the glass panel's solid fill and rim; the tint over the glass is CupertinoPopoverTintBrush.
	-->
	<ResourceDictionary.ThemeDictionaries>
		<ResourceDictionary x:Key="Light">
			<StaticResource x:Key="FlyoutPresenterBackground" ResourceKey="SurfaceBrush" />
			<StaticResource x:Key="FlyoutPresenterBorderBrush" ResourceKey="OutlineVariantBrush" />
		</ResourceDictionary>
		<ResourceDictionary x:Key="Default">
			<StaticResource x:Key="FlyoutPresenterBackground" ResourceKey="SurfaceBrush" />
			<StaticResource x:Key="FlyoutPresenterBorderBrush" ResourceKey="OutlineVariantBrush" />
		</ResourceDictionary>
	</ResourceDictionary.ThemeDictionaries>

	<!-- The iOS 26 popover and menu radius; the hairline is Apple's 0.5 pt separator. -->
	<CornerRadius x:Key="CupertinoPopoverCornerRadius">26</CornerRadius>
	<Thickness x:Key="CupertinoPopoverPadding">16</Thickness>
	<Thickness x:Key="CupertinoHairlineThickness">0.5</Thickness>

	<Style x:Key="CupertinoFlyoutPresenterStyle"
		   TargetType="FlyoutPresenter">
		<Setter Property="Background" Value="{ThemeResource FlyoutPresenterBackground}" />
		<Setter Property="BorderBrush" Value="{ThemeResource FlyoutPresenterBorderBrush}" />
		<Setter Property="BorderThickness" Value="{StaticResource CupertinoHairlineThickness}" />
		<Setter Property="CornerRadius" Value="{StaticResource CupertinoPopoverCornerRadius}" />
		<Setter Property="Padding" Value="{StaticResource CupertinoPopoverPadding}" />
		<Setter Property="IsTabStop" Value="False" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="VerticalContentAlignment" Value="Stretch" />
		<Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled" />
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
		<Setter Property="ScrollViewer.VerticalScrollMode" Value="Auto" />
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
		<Setter Property="ScrollViewer.ZoomMode" Value="Disabled" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="FlyoutPresenter">
					<Grid>
						<uc:GlassPanel Material="Thick"
									   Tint="{ThemeResource CupertinoPopoverTintBrush}"
									   Background="{TemplateBinding Background}"
									   BorderBrush="{TemplateBinding BorderBrush}"
									   BorderThickness="{TemplateBinding BorderThickness}"
									   CornerRadius="{TemplateBinding CornerRadius}" />
						<Border CornerRadius="{TemplateBinding CornerRadius}">
							<ScrollViewer x:Name="ScrollViewer"
										  ZoomMode="{TemplateBinding ScrollViewer.ZoomMode}"
										  HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
										  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
										  VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
										  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
										  AutomationProperties.AccessibilityView="Raw">
								<ContentPresenter Content="{TemplateBinding Content}"
												  ContentTemplate="{TemplateBinding ContentTemplate}"
												  ContentTransitions="{TemplateBinding ContentTransitions}"
												  Margin="{TemplateBinding Padding}"
												  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
												  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
							</ScrollViewer>
						</Border>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
