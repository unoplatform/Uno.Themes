<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!-- Lightweight styling of the filled style. The bordered style keeps its Cupertino* keys. -->
	<ResourceDictionary.ThemeDictionaries>
		<ResourceDictionary x:Key="Light">
			<StaticResource x:Key="CupertinoPasswordBoxBorderBrush" ResourceKey="CupertinoFieldBorderBrush" />
			<StaticResource x:Key="FilledPasswordBoxForeground" ResourceKey="OnSurfaceBrush" />
			<StaticResource x:Key="FilledPasswordBoxBackground" ResourceKey="SecondaryContainerBrush" />
			<StaticResource x:Key="FilledPasswordBoxPlaceholderForeground" ResourceKey="OnSurfaceVariantBrush" />
		</ResourceDictionary>
		<ResourceDictionary x:Key="Default">
			<StaticResource x:Key="CupertinoPasswordBoxBorderBrush" ResourceKey="CupertinoFieldBorderBrush" />
			<StaticResource x:Key="FilledPasswordBoxForeground" ResourceKey="OnSurfaceBrush" />
			<StaticResource x:Key="FilledPasswordBoxBackground" ResourceKey="SecondaryContainerBrush" />
			<StaticResource x:Key="FilledPasswordBoxPlaceholderForeground" ResourceKey="OnSurfaceVariantBrush" />
		</ResourceDictionary>
		<ResourceDictionary x:Key="HighContrast">
			<StaticResource x:Key="CupertinoPasswordBoxBorderBrush" ResourceKey="CupertinoFieldBorderBrush" />
		</ResourceDictionary>
	</ResourceDictionary.ThemeDictionaries>

	<CornerRadius x:Key="CupertinoPasswordBoxCornerRadius">10</CornerRadius>

	<Style x:Key="CupertinoPasswordBoxStyle"
		   TargetType="PasswordBox">
		<Setter Property="FontFamily" Value="{ThemeResource DefaultFontFamily}" />
		<Setter Property="FontSize" Value="17" />
		<Setter Property="Background" Value="Transparent" />
		<!-- Issue with updating foreground on theme change https://github.com/unoplatform/Uno.Themes/issues/522 -->
		<Setter Property="Foreground" Value="{ThemeResource CupertinoLabelBrush}" />
		<Setter Property="BorderBrush" Value="{ThemeResource CupertinoPasswordBoxBorderBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="CornerRadius" Value="{StaticResource CupertinoPasswordBoxCornerRadius}" />

		<Setter Property="HorizontalContentAlignment" Value="Left" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="Padding" Value="12,0" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="PasswordBox">
					<Grid x:Name="Root">

						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />
								<VisualState x:Name="PointerOver" />
								<VisualState x:Name="Pressed" />

								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="Root.Opacity" Value="{StaticResource CupertinoDisableStateOpacity}" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Focused" />
							</VisualStateGroup>
							<VisualStateGroup x:Name="HeaderStates">
								<VisualState x:Name="NotEmpty" />
								<VisualState x:Name="Empty">
									<VisualState.Setters>
										<Setter Target="PlaceholderElement.Opacity" Value="1" />
									</VisualState.Setters>
									<VisualState.StateTriggers>
										<StateTrigger IsActive="{Binding Password, Converter={StaticResource EmptyToTrueConverter}, RelativeSource={RelativeSource TemplatedParent}}" />
									</VisualState.StateTriggers>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="*" />
						</Grid.RowDefinitions>
						<ContentPresenter x:Name="HeaderContentPresenter"
										  Content="{TemplateBinding Header}"
										  ContentTemplate="{TemplateBinding HeaderTemplate}"
										  FontSize="13"
										  Foreground="{TemplateBinding Foreground}"
										  TextWrapping="Wrap"
										  Margin="0,0,0,4"
										  Visibility="{Binding Header, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToCollapsedConverter}}" />
						<Grid Grid.Row="1"
							  Background="{TemplateBinding Background}"
							  BorderBrush="{TemplateBinding BorderBrush}"
							  BorderThickness="{TemplateBinding BorderThickness}"
							  CornerRadius="{TemplateBinding CornerRadius}"
							  Padding="{TemplateBinding Padding}"
							  MinHeight="44">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>

							<ScrollViewer x:Name="ContentElement"
										  Grid.Column="1"
										  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
										  HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
										  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
										  VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
										  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
										  IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}"
										  IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}"
										  IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
										  IsTabStop="False"
										  AutomationProperties.AccessibilityView="Raw"
										  ZoomMode="Disabled" />

							<TextBlock x:Name="PlaceholderElement"
									   Grid.Column="1"
									   Opacity="0"
									   Foreground="{ThemeResource CupertinoPlaceholderTextBrush}"
									   Text="{TemplateBinding PlaceholderText}"
									   Style="{StaticResource CupertinoBody}"
									   HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
									   VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
									   IsHitTestVisible="False"
									   RenderTransformOrigin="0,0.5" />
						</Grid>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- A borderless field on the gray fill: the look of a search field or of a field inside a grouped list. -->
	<Style x:Key="CupertinoFilledPasswordBoxStyle"
		   BasedOn="{StaticResource CupertinoPasswordBoxStyle}"
		   TargetType="PasswordBox">
		<Setter Property="Foreground" Value="{ThemeResource FilledPasswordBoxForeground}" />
		<Setter Property="Background" Value="{ThemeResource FilledPasswordBoxBackground}" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="CornerRadius" Value="{StaticResource CupertinoFilledTextBoxCornerRadius}" />
	</Style>

</ResourceDictionary>
