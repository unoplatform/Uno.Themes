<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:not_win="http://uno.ui/not_win"
					xmlns:uno="using:Uno.UI.Xaml.Controls"
					xmlns:uc="using:Uno.Cupertino"
					mc:Ignorable="not_win">
	<ResourceDictionary.ThemeDictionaries>
		<ResourceDictionary x:Key="Light">
			<Thickness x:Key="MaterialDatePickerHostPadding">24,24,8,8</Thickness>
			<StaticResource x:Key="CupertinoDatePickerBorderBrush" ResourceKey="CupertinoFieldBorderBrush" />
			<StaticResource x:Key="CupertinoDatePickerFlyoutPresenterHighlightFill" ResourceKey="CupertinoQuinaryGrayBrush" />
		</ResourceDictionary>
		<ResourceDictionary x:Key="Default">
			<Thickness x:Key="MaterialDatePickerHostPadding">24,24,8,8</Thickness>
			<StaticResource x:Key="CupertinoDatePickerBorderBrush" ResourceKey="CupertinoFieldBorderBrush" />
			<StaticResource x:Key="CupertinoDatePickerFlyoutPresenterHighlightFill" ResourceKey="CupertinoQuinaryGrayBrush" />
		</ResourceDictionary>
		<ResourceDictionary x:Key="HighContrast">
			<StaticResource x:Key="CupertinoDatePickerBorderBrush" ResourceKey="CupertinoFieldBorderBrush" />
			<StaticResource x:Key="CupertinoDatePickerFlyoutPresenterHighlightFill" ResourceKey="CupertinoQuinaryGrayBrush" />
		</ResourceDictionary>
	</ResourceDictionary.ThemeDictionaries>

	<x:Double x:Key="CupertinoDateTimeFlyoutBorderThickness">1</x:Double>
	<x:Double x:Key="MaterialDatePickerSpacerThemeWidth">1</x:Double>

	<StaticResource x:Key="CupertinoDatePickerCornerRadius" ResourceKey="Radius550CornerRadius" />

	<!-- DatePicker Style -->
	<Style x:Key="CupertinoDatePickerFlyoutButtonStyle"
		   TargetType="Button">
		<Setter Property="FontFamily" Value="{ThemeResource BodyLargeFontFamily}" />
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="Padding" Value="0" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="Button">

					<Grid x:Name="RootGrid"
						  Background="{TemplateBinding Background}">
						<!-- Label -->
						<ContentPresenter x:Name="ContentPresenter"
										  Margin="{TemplateBinding Padding}"
										  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
										  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
										  FontFamily="{TemplateBinding FontFamily}"
										  AutomationProperties.AccessibilityView="Raw"
										  Content="{TemplateBinding Content}"
										  ContentTemplate="{TemplateBinding ContentTemplate}"
										  ContentTransitions="{TemplateBinding ContentTransitions}" />

						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />
								<VisualState x:Name="PointerOver" />
								<VisualState x:Name="Pressed">
									<VisualState.Setters>
										<Setter Target="RootGrid.Opacity" Value="{StaticResource CupertinoButtonPressedOpacity}" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="RootGrid.Opacity" Value="0.65" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="CupertinoDatePickerFlyoutPresenterStyle"
		   TargetType="DatePickerFlyoutPresenter">
		<Setter Property="Width" Value="296" />
		<Setter Property="MinWidth" Value="296" />
		<Setter Property="MaxHeight" Value="398" />
		<Setter Property="IsTabStop" Value="False" />
		<Setter Property="FontFamily" Value="{ThemeResource BodyLargeFontFamily}" />
		<Setter Property="FontWeight" Value="Normal" />
		<!-- Wheel rows read at 20 pt, close to the iOS picker's 23 pt, inside the 44 px selection band. -->
		<Setter Property="FontSize" Value="{ThemeResource HeadlineSmallFontSize}" />
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="AutomationProperties.AutomationId" Value="DatePickerFlyoutPresenter" />
		<Setter Property="BorderThickness" Value="{StaticResource CupertinoDateTimeFlyoutBorderThickness}" />
		<Setter Property="BorderBrush" Value="{ThemeResource CupertinoDatePickerBorderBrush}" />
		<Setter Property="CornerRadius" Value="{ThemeResource Radius650CornerRadius}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="DatePickerFlyoutPresenter">
					<Grid MaxHeight="398">
						<uc:GlassPanel Material="Thick"
									   BorderBrush="{TemplateBinding BorderBrush}"
									   BorderThickness="{TemplateBinding BorderThickness}"
									   Tint="{ThemeResource CupertinoPopoverTintBrush}"
									   CornerRadius="{TemplateBinding CornerRadius}" />
						<Border x:Name="Background"
								CornerRadius="{TemplateBinding CornerRadius}">
							<Grid x:Name="ContentPanel">

								<Grid.RowDefinitions>
									<RowDefinition Height="Auto" />
									<RowDefinition Height="*" />
								</Grid.RowDefinitions>
								<Grid x:Name="AcceptDismissHostGrid"
									  Height="{ThemeResource DatePickerFlyoutPresenterAcceptDismissHostGridHeight}"
									  Background="{TemplateBinding Background}">

									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto" />
										<ColumnDefinition Width="*" />
										<ColumnDefinition Width="Auto" />
									</Grid.ColumnDefinitions>
									<Rectangle Grid.ColumnSpan="3"
											   Height="{ThemeResource MaterialDatePickerSpacerThemeWidth}"
											   VerticalAlignment="Bottom"
											   Fill="{ThemeResource CupertinoOpaqueSeparatorBrush}" />
									<Button x:Name="DismissButton"
											x:Uid="DatePickerFlyoutDismissButton"
											Grid.Column="0"
											Margin="{ThemeResource Space300LeftThickness}"
											Content="Cancel"
											Style="{StaticResource CupertinoButtonStyle}" />
									<TextBlock x:Name="DatePickerFlyoutHeader"
											   Grid.Column="1"
											   x:Uid="DatePickerFlyoutDismissButton"
											   Text="Enter Date"
											   HorizontalAlignment="Center"
											   VerticalAlignment="Center"
											   TextWrapping="NoWrap"
											   TextTrimming="CharacterEllipsis"
											   Style="{StaticResource CupertinoTitleMedium}" />
									<Button x:Name="AcceptButton"
											x:Uid="DatePickerFlyoutAcceptButton"
											Grid.Column="2"
											Margin="{ThemeResource Space300RightThickness}"
											Content="Done"
											Style="{StaticResource CupertinoButtonStyle}" />
								</Grid>
								<Grid x:Name="PickerHostGrid"
									  Grid.Row="1"
									  Background="{TemplateBinding Background}">

									<Grid.ColumnDefinitions>
										<ColumnDefinition x:Name="DayColumn"
														  Width="78*" />
										<ColumnDefinition x:Name="FirstSpacerColumn"
														  Width="Auto" />
										<ColumnDefinition x:Name="MonthColumn"
														  Width="132*" />
										<ColumnDefinition x:Name="SecondSpacerColumn"
														  Width="Auto" />
										<ColumnDefinition x:Name="YearColumn"
														  Width="78*" />
									</Grid.ColumnDefinitions>
									<Rectangle x:Name="HighlightRect"
											   Grid.Column="0"
											   Grid.ColumnSpan="5"
											   Height="{ThemeResource DatePickerFlyoutPresenterHighlightHeight}"
											   Margin="{ThemeResource Space150HorizontalThickness}"
											   VerticalAlignment="Center"
											   Fill="{ThemeResource CupertinoDatePickerFlyoutPresenterHighlightFill}"
											   RadiusX="18"
											   RadiusY="18" />
									<Rectangle x:Name="FirstPickerSpacing"
											   Grid.Column="1"
											   Width="0"
											   HorizontalAlignment="Center"
											   Fill="{ThemeResource CupertinoOpaqueSeparatorBrush}" />
									<Rectangle x:Name="SecondPickerSpacing"
											   Grid.Column="3"
											   Width="0"
											   HorizontalAlignment="Center"
											   Fill="{ThemeResource CupertinoOpaqueSeparatorBrush}" />
								</Grid>

							</Grid>
						</Border>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="CupertinoDatePickerStyle"
		   TargetType="DatePicker">
		<Setter Property="MinWidth" Value="0" />
		<Setter Property="FontSize" Value="{ThemeResource BodyLargeFontSize}" />
		<Setter Property="FontWeight" Value="{ThemeResource BodyLargeFontWeight}" />
		<Setter Property="CharacterSpacing" Value="{ThemeResource BodyLargeCharacterSpacing}" />
		<Setter Property="FontFamily" Value="{ThemeResource BodyLargeFontFamily}" />
		<Setter Property="Background" Value="{ThemeResource CupertinoQuinaryGrayBrush}" />
		<Setter Property="Foreground" Value="{ThemeResource PrimaryBrush}" />
		<Setter Property="BorderBrush" Value="{ThemeResource CupertinoDatePickerBorderBrush}" />
		<Setter Property="HorizontalAlignment" Value="Left" />
		<Setter Property="CornerRadius" Value="{ThemeResource CupertinoDatePickerCornerRadius}" />
		<not_win:Setter Property="FlyoutPresenterStyle"
						Value="{StaticResource CupertinoDatePickerFlyoutPresenterStyle}" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="DatePicker">

					<!-- Root Grid -->
					<Grid x:Name="LayoutRoot">
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="*" />
						</Grid.RowDefinitions>
						<!-- Header -->
						<TextBlock x:Name="HeaderTextBlock"
								   FontSize="{ThemeResource CaptionLargeFontSize}"
								   FontFamily="{ThemeResource CaptionLargeFontFamily}"
								   FontWeight="{ThemeResource CaptionLargeFontWeight}"
								   CharacterSpacing="{ThemeResource CaptionLargeCharacterSpacing}"
								   Margin="{ThemeResource Space100BottomThickness}"
								   HorizontalAlignment="Stretch"
								   VerticalAlignment="Top"
								   Foreground="{ThemeResource OnSurfaceBrush}"
								   Style="{StaticResource CupertinoFootnote}"
								   Text="{TemplateBinding Header}"
								   TextWrapping="Wrap"
								   Visibility="{Binding Header, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToCollapsedConverter}}" />
						<!-- Flyout Button -->
						<Button x:Name="FlyoutButton"
								Grid.Row="1"
								HorizontalAlignment="Stretch"
								HorizontalContentAlignment="Stretch"
								IsEnabled="{TemplateBinding IsEnabled}"
								Style="{StaticResource CupertinoDatePickerFlyoutButtonStyle}"
								MinHeight="{ThemeResource ControlHeightMediumLarge}"
								UseSystemFocusVisuals="{TemplateBinding UseSystemFocusVisuals}">

							<Grid x:Name="FlyoutButtonContentGrid"
								  MinHeight="{ThemeResource ControlHeightMediumLarge}"
								  VerticalAlignment="Top"
								  Background="{TemplateBinding Background}"
								  BorderBrush="{TemplateBinding BorderBrush}"
								  BorderThickness="0"
								  CornerRadius="{TemplateBinding CornerRadius}">

								<!-- DateText -->
								<TextBlock x:Name="DateText"
										   Foreground="{TemplateBinding Foreground}"
										   Margin="{ThemeResource Space400HorizontalThickness}"
										   VerticalAlignment="Center"
										   Style="{StaticResource CupertinoBody}"
										   Text="{Binding SelectedDate, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource StringFormatConverter}, ConverterParameter=' {0:d}'}" />

								<!-- PlaceholderText -->
								<TextBlock x:Name="PlaceholderText"
										   x:Uid="CupertinoDatePickerPlaceholder"
										   Margin="{ThemeResource Space400HorizontalThickness}"
										   VerticalAlignment="Center"
										   Foreground="{TemplateBinding Foreground}"
										   Style="{StaticResource CupertinoBody}"
										   Text="Choose a date"
										   Visibility="Collapsed" />

								<!-- Removing this cause trouble with the DatePicker code -->
								<TextBlock x:Name="DayTextBlock"
										   Opacity="0" />
							</Grid>
						</Button>

						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />

								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="DateText.Foreground" Value="{ThemeResource CupertinoPrimaryGrayBrush}" />
										<Setter Target="HeaderTextBlock.Foreground" Value="{ThemeResource CupertinoPrimaryGrayBrush}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<VisualStateGroup x:Name="HasDateStates">
								<VisualState x:Name="HasDate" />
								<VisualState x:Name="HasNoDate">
									<VisualState.Setters>
										<Setter Target="DateText.Visibility" Value="Collapsed" />
										<Setter Target="PlaceholderText.Visibility" Value="Visible" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
