<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:uc="using:Uno.Cupertino">

	<!--
		An inset-grouped list: the ListView is the group, a rounded surface; a row is 44 px with a hairline
		separator inset from the leading edge. Rows highlight with the gray fill, a selected row with the
		selection tint, and a disabled row dims. No glass: a list is content.
	-->
	<ResourceDictionary.ThemeDictionaries>
		<ResourceDictionary x:Key="Light">
			<StaticResource x:Key="ListViewBackground" ResourceKey="SurfaceBrush" />
			<StaticResource x:Key="ListViewBorderBrush" ResourceKey="OutlineVariantBrush" />
			<StaticResource x:Key="ListViewItemForeground" ResourceKey="OnSurfaceBrush" />
			<!-- Translucent system fills read on any row surface, in both appearances; a selected iPhone row is neutral gray, never the accent. -->
			<StaticResource x:Key="ListViewItemBackgroundPointerOver" ResourceKey="CupertinoQuaternarySystemFillBrush" />
			<StaticResource x:Key="ListViewItemBackgroundPressed" ResourceKey="CupertinoSystemFillBrush" />
			<StaticResource x:Key="ListViewItemBackgroundSelected" ResourceKey="CupertinoSystemFillBrush" />
			<StaticResource x:Key="ListViewItemSeparatorBrush" ResourceKey="CupertinoSeparatorBrush" />
		</ResourceDictionary>
		<ResourceDictionary x:Key="Default">
			<StaticResource x:Key="ListViewBackground" ResourceKey="SurfaceBrush" />
			<StaticResource x:Key="ListViewBorderBrush" ResourceKey="OutlineVariantBrush" />
			<StaticResource x:Key="ListViewItemForeground" ResourceKey="OnSurfaceBrush" />
			<!-- Translucent system fills read on any row surface, in both appearances; a selected iPhone row is neutral gray, never the accent. -->
			<StaticResource x:Key="ListViewItemBackgroundPointerOver" ResourceKey="CupertinoQuaternarySystemFillBrush" />
			<StaticResource x:Key="ListViewItemBackgroundPressed" ResourceKey="CupertinoSystemFillBrush" />
			<StaticResource x:Key="ListViewItemBackgroundSelected" ResourceKey="CupertinoSystemFillBrush" />
			<StaticResource x:Key="ListViewItemSeparatorBrush" ResourceKey="CupertinoSeparatorBrush" />
		</ResourceDictionary>
	</ResourceDictionary.ThemeDictionaries>

	<!-- Density scales row padding and separator insets; the row height stays independent. -->
	<StaticResource x:Key="CupertinoRowMinHeight" ResourceKey="ControlHeightMediumLarge" />
	<StaticResource x:Key="CupertinoRowPadding" ResourceKey="Space400HorizontalThickness" />
	<StaticResource x:Key="CupertinoSeparatorInset" ResourceKey="Space400LeftThickness" />
	<!-- The iOS 26 inset-grouped section radius. -->
	<StaticResource x:Key="CupertinoGroupCornerRadius" ResourceKey="Radius650CornerRadius" />

	<Style x:Key="CupertinoListViewItemStyle"
		   TargetType="ListViewItem">
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="Foreground" Value="{ThemeResource ListViewItemForeground}" />
		<Setter Property="FontFamily" Value="{ThemeResource BodyLargeFontFamily}" />
		<Setter Property="FontSize" Value="{ThemeResource BodyLargeFontSize}" />
		<Setter Property="FontWeight" Value="{ThemeResource BodyLargeFontWeight}" />
		<Setter Property="CharacterSpacing" Value="{ThemeResource BodyLargeCharacterSpacing}" />
		<Setter Property="MinHeight" Value="{ThemeResource CupertinoRowMinHeight}" />
		<Setter Property="Padding" Value="{ThemeResource CupertinoRowPadding}" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="TabNavigation" Value="Local" />
		<Setter Property="IsHoldingEnabled" Value="True" />
		<Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
		<Setter Property="FocusVisualMargin" Value="0" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListViewItem">
					<Grid x:Name="ContentBorder"
						  Background="{TemplateBinding Background}"
						  MinHeight="{TemplateBinding MinHeight}">
						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />
								<VisualState x:Name="PointerOver">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource ListViewItemBackgroundPointerOver}" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="Pressed">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource ListViewItemBackgroundPressed}" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="Selected">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource ListViewItemBackgroundSelected}" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="PointerOverSelected">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource ListViewItemBackgroundSelected}" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="PressedSelected">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Background" Value="{ThemeResource ListViewItemBackgroundSelected}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
							<VisualStateGroup x:Name="DisabledStates">
								<VisualState x:Name="Enabled" />
								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="ContentBorder.Opacity" Value="{StaticResource CupertinoDisableStateOpacity}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<ContentPresenter CharacterSpacing="{TemplateBinding CharacterSpacing}"
										  uc:CupertinoTypography.BindCharacterSpacing="True"
										  x:Name="ContentPresenter"
										  Content="{TemplateBinding Content}"
										  ContentTemplate="{TemplateBinding ContentTemplate}"
										  ContentTransitions="{TemplateBinding ContentTransitions}"
										  Margin="{TemplateBinding Padding}"
										  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
										  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />

						<!-- Every row draws one; the group's ItemsPresenter is pulled up by a pixel so the last row's hairline falls outside the group. -->
						<Rectangle x:Name="Separator"
								   Fill="{ThemeResource ListViewItemSeparatorBrush}"
								   Height="0.5"
								   Margin="{ThemeResource CupertinoSeparatorInset}"
								   UseLayoutRounding="False"
								   VerticalAlignment="Bottom" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="CupertinoListViewStyle"
		   TargetType="ListView">
		<Setter Property="Background" Value="{ThemeResource ListViewBackground}" />
		<Setter Property="BorderBrush" Value="{ThemeResource ListViewBorderBrush}" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="CornerRadius" Value="{ThemeResource CupertinoGroupCornerRadius}" />
		<Setter Property="IsTabStop" Value="False" />
		<Setter Property="TabNavigation" Value="Once" />
		<Setter Property="IsSwipeEnabled" Value="True" />
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
		<Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled" />
		<Setter Property="ScrollViewer.IsHorizontalRailEnabled" Value="False" />
		<Setter Property="ScrollViewer.VerticalScrollMode" Value="Enabled" />
		<Setter Property="ScrollViewer.IsVerticalRailEnabled" Value="True" />
		<Setter Property="ScrollViewer.ZoomMode" Value="Disabled" />
		<Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="False" />
		<Setter Property="ScrollViewer.BringIntoViewOnFocusChange" Value="True" />
		<Setter Property="ItemContainerStyle" Value="{StaticResource CupertinoListViewItemStyle}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListView">
					<Border x:Name="Group"
							Background="{TemplateBinding Background}"
							BorderBrush="{TemplateBinding BorderBrush}"
							BorderThickness="{TemplateBinding BorderThickness}"
							CornerRadius="{TemplateBinding CornerRadius}">
						<ScrollViewer x:Name="ScrollViewer"
									  TabNavigation="{TemplateBinding TabNavigation}"
									  HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
									  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
									  IsHorizontalScrollChainingEnabled="{TemplateBinding ScrollViewer.IsHorizontalScrollChainingEnabled}"
									  VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
									  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
									  IsVerticalScrollChainingEnabled="{TemplateBinding ScrollViewer.IsVerticalScrollChainingEnabled}"
									  IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}"
									  IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}"
									  ZoomMode="{TemplateBinding ScrollViewer.ZoomMode}"
									  IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
									  BringIntoViewOnFocusChange="{TemplateBinding ScrollViewer.BringIntoViewOnFocusChange}"
									  AutomationProperties.AccessibilityView="Raw">
							<!-- The negative margin hides the last row's separator: an inset group has no line under its last row. -->
							<ItemsPresenter Margin="0,0,0,-1"
											Header="{TemplateBinding Header}"
											HeaderTemplate="{TemplateBinding HeaderTemplate}"
											HeaderTransitions="{TemplateBinding HeaderTransitions}"
											Footer="{TemplateBinding Footer}"
											FooterTemplate="{TemplateBinding FooterTemplate}"
											FooterTransitions="{TemplateBinding FooterTransitions}"
											Padding="{TemplateBinding Padding}" />
						</ScrollViewer>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
