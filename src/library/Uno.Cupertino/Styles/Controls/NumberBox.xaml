<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:uc="using:Uno.Cupertino"
					xmlns:muxc="using:Microsoft.UI.Xaml.Controls">
	<ResourceDictionary.ThemeDictionaries>
		<ResourceDictionary x:Key="Light">
			<StaticResource x:Key="CupertinoHeaderFontSize" ResourceKey="CaptionLargeFontSize" />
			<StaticResource x:Key="CupertinoTextBoxBorderBrush" ResourceKey="CupertinoFieldBorderBrush" />
		</ResourceDictionary>
		<ResourceDictionary x:Key="Default">
			<StaticResource x:Key="CupertinoHeaderFontSize" ResourceKey="CaptionLargeFontSize" />
			<StaticResource x:Key="CupertinoTextBoxBorderBrush" ResourceKey="CupertinoFieldBorderBrush" />
		</ResourceDictionary>
		<ResourceDictionary x:Key="HighContrast">
			<StaticResource x:Key="CupertinoHeaderFontSize" ResourceKey="CaptionLargeFontSize" />
			<StaticResource x:Key="CupertinoTextBoxBorderBrush" ResourceKey="CupertinoFieldBorderBrush" />
		</ResourceDictionary>
	</ResourceDictionary.ThemeDictionaries>

	<x:String x:Key="PlusGlyphPathStyle">M0.578125 5.67236H4.29688V9.47412H5.6416V5.67236H9.36035V4.32764H5.6416V0.525879H4.29688V4.32764H0.578125V5.67236Z</x:String>
	<x:String x:Key="MinusGlyphPathStyle">M0 1.67236H5.57812V0.327637H0V1.67236Z</x:String>

	<StaticResource x:Key="CupertinoCornerRadius" ResourceKey="Radius250CornerRadius" />
	<!-- Horizontal only: the 44 pt spin buttons set the frame's height, as a TextBox is 44 pt. -->
	<Thickness x:Key="CupertinoNumberBoxPadding">7,0,7,0</Thickness>

	<Style x:Name="CupertinoNumberBoxStyle"
		   TargetType="muxc:NumberBox">
		<Setter Property="FontFamily" Value="{ThemeResource BodyLargeFontFamily}" />
		<Setter Property="FontSize" Value="{ThemeResource BodyLargeFontSize}" />
		<Setter Property="FontWeight" Value="{ThemeResource BodyLargeFontWeight}" />
		<Setter Property="CharacterSpacing" Value="{ThemeResource BodyLargeCharacterSpacing}" />
		<Setter Property="Background" Value="{ThemeResource CupertinoSystemBackgroundBrush}" />
		<Setter Property="CornerRadius" Value="{ThemeResource CupertinoCornerRadius}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="BorderBrush" Value="{ThemeResource CupertinoTextBoxBorderBrush}" />
		<Setter Property="MinHeight" Value="{ThemeResource ControlHeightMediumLarge}" />
		<Setter Property="Padding" Value="{StaticResource CupertinoNumberBoxPadding}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="muxc:NumberBox">
					<!-- The header sits above the frame, as for every other field; iOS draws no header inside a box. -->
					<Grid x:Name="RootGrid">
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="*" />
						</Grid.RowDefinitions>
						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />
								<VisualState x:Name="PointerOver" />
								<VisualState x:Name="Pressed" />
								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="RootGrid.Opacity" Value="{StaticResource CupertinoDisableStateOpacity}" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
							<VisualStateGroup x:Name="SpinButtonStates">
								<VisualState x:Name="SpinButtonsCollapsed" />

								<VisualState x:Name="SpinButtonsVisible">
									<VisualState.Setters>
										<Setter Target="DownSpinButton.Visibility" Value="Visible" />
										<Setter Target="UpSpinButton.Visibility" Value="Visible" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

						</VisualStateManager.VisualStateGroups>

						<ContentPresenter uc:CupertinoTypography.BindCharacterSpacing="True"
										  x:Name="Header"
										  Content="{TemplateBinding Header}"
										  ContentTemplate="{TemplateBinding HeaderTemplate}"
										  FontSize="{ThemeResource CupertinoHeaderFontSize}"
										  FontFamily="{ThemeResource CaptionLargeFontFamily}"
										  FontWeight="{ThemeResource CaptionLargeFontWeight}"
										  CharacterSpacing="{ThemeResource CaptionLargeCharacterSpacing}"
										  Foreground="{ThemeResource CupertinoHeaderForegroundBrush}"
										  Margin="{ThemeResource Space100BottomThickness}"
										  TextWrapping="Wrap"
										  Visibility="{Binding Header, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToCollapsedConverter}}" />

						<Border x:Name="ContentBorder"
								Grid.Row="1"
								MinHeight="{TemplateBinding MinHeight}"
								Background="{TemplateBinding Background}"
								BorderBrush="{TemplateBinding BorderBrush}"
								BorderThickness="{TemplateBinding BorderThickness}"
								CornerRadius="{TemplateBinding CornerRadius}" />

						<Grid x:Name="GridRoot"
							  Grid.Row="1"
							  Margin="{TemplateBinding Padding}">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>

							<TextBox x:Name="InputBox"
									 Grid.Column="0"
									 Style="{StaticResource NumberBoxTextBoxStyle}"
									 FontFamily="{TemplateBinding FontFamily}"
									 FontSize="{TemplateBinding FontSize}"
									 FontWeight="{TemplateBinding FontWeight}"
									 CharacterSpacing="{TemplateBinding CharacterSpacing}"
									 Text="{TemplateBinding Text}"
									 PlaceholderText="{TemplateBinding PlaceholderText}" />

							<StackPanel CornerRadius="{ThemeResource Radius550CornerRadius}"
										VerticalAlignment="Center"
										Background="{ThemeResource CupertinoQuinaryGrayBrush}"
										Orientation="Horizontal"
										Grid.Column="1">

								<RepeatButton x:Name="DownSpinButton"
											  Grid.Column="1"
											  Style="{StaticResource NumberBoxMinusButtonStyle}" />
								<Rectangle Width="1"
										   Height="12"
										   Fill="{ThemeResource CupertinoDetailsLightBrush}" />
								<RepeatButton x:Name="UpSpinButton"
											  Style="{StaticResource NumberBoxPlusButtonStyle}" />
							</StackPanel>
						</Grid>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="NumberBoxPlusButtonStyle"
		   TargetType="RepeatButton">
		<Style.Setters>
			<Setter Property="IsTabStop" Value="False" />
			<Setter Property="Width" Value="{ThemeResource ControlHeightMediumLarge}" />
			<Setter Property="Height" Value="{ThemeResource ControlHeightMediumLarge}" />
			<Setter Property="FontSize" Value="22" />
			<Setter Property="Padding" Value="{ThemeResource Space175Thickness}" />
			<Setter Property="VerticalAlignment" Value="Center" />
			<Setter Property="HorizontalAlignment" Value="Center" />
			<Setter Property="Background" Value="{ThemeResource CupertinoQuinaryGrayBrush}" />
			<Setter Property="Foreground" Value="{ThemeResource CupertinoLabelBrush}" />
			<Setter Property="BorderThickness" Value="0" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="RepeatButton">
						<Grid x:Name="ButtonLayoutGrid"
							  Background="Transparent">

							<VisualStateManager.VisualStateGroups>
								<VisualStateGroup x:Name="CommonStates">
									<VisualState x:Name="Normal" />
									<VisualState x:Name="PointerOver" />
									<VisualState x:Name="Pressed">
										<VisualState.Setters>
											<Setter Target="PressedOverlay.Opacity" Value="{StaticResource CupertinoFocusedOpacity}" />
										</VisualState.Setters>
									</VisualState>
									<VisualState x:Name="Disabled">
										<VisualState.Setters>
											<Setter Target="ButtonLayoutGrid.Opacity" Value="{StaticResource CupertinoDisableStateOpacity}" />
										</VisualState.Setters>
									</VisualState>
								</VisualStateGroup>
							</VisualStateManager.VisualStateGroups>

							<Path x:Name="GlyphElement"
								  Data="{StaticResource PlusGlyphPathStyle}"
								  Fill="{TemplateBinding Foreground}"
								  Margin="{TemplateBinding Padding}"
								  VerticalAlignment="Center"
								  HorizontalAlignment="Center"
								  Width="14"
								  Stretch="Uniform"
								  AutomationProperties.AccessibilityView="Raw" />

							<Border x:Name="PressedOverlay"
									Background="{TemplateBinding Foreground}"
									Opacity="0" />
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style.Setters>
	</Style>

	<Style x:Key="NumberBoxMinusButtonStyle"
		   TargetType="RepeatButton">
		<Style.Setters>
			<Setter Property="IsTabStop" Value="False" />
			<Setter Property="Width" Value="{ThemeResource ControlHeightMediumLarge}" />
			<Setter Property="Height" Value="{ThemeResource ControlHeightMediumLarge}" />
			<Setter Property="FontSize" Value="22" />
			<Setter Property="Padding" Value="{ThemeResource Space175Thickness}" />
			<Setter Property="VerticalAlignment" Value="Center" />
			<Setter Property="HorizontalAlignment" Value="Center" />
			<Setter Property="Background" Value="{ThemeResource CupertinoQuinaryGrayBrush}" />
			<Setter Property="Foreground" Value="{ThemeResource CupertinoLabelBrush}" />
			<Setter Property="BorderThickness" Value="0" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="RepeatButton">
						<Grid x:Name="ButtonLayoutGrid"
							  Background="Transparent">

							<VisualStateManager.VisualStateGroups>
								<VisualStateGroup x:Name="CommonStates">
									<VisualState x:Name="Normal" />
									<VisualState x:Name="PointerOver" />
									<VisualState x:Name="Pressed">
										<VisualState.Setters>
											<Setter Target="PressedOverlay.Opacity" Value="{StaticResource CupertinoFocusedOpacity}" />
										</VisualState.Setters>
									</VisualState>
									<VisualState x:Name="Disabled">
										<VisualState.Setters>
											<Setter Target="ButtonLayoutGrid.Opacity" Value="{StaticResource CupertinoDisableStateOpacity}" />
										</VisualState.Setters>
									</VisualState>
								</VisualStateGroup>
							</VisualStateManager.VisualStateGroups>

							<Path x:Name="GlyphElement"
								  Data="{StaticResource MinusGlyphPathStyle}"
								  Fill="{TemplateBinding Foreground}"
								  Margin="{TemplateBinding Padding}"
								  VerticalAlignment="Center"
								  HorizontalAlignment="Center"
								  Width="14"
								  Stretch="Uniform"
								  AutomationProperties.AccessibilityView="Raw" />

							<Border x:Name="PressedOverlay"
									Background="{TemplateBinding Foreground}"
									Opacity="0" />
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style.Setters>
	</Style>

	<Style x:Key="NumberBoxTextBoxStyle"
		   TargetType="TextBox">
		<Setter Property="FontFamily" Value="{ThemeResource BodyLargeFontFamily}" />
		<Setter Property="FontSize" Value="{ThemeResource BodyLargeFontSize}" />
		<Setter Property="FontWeight" Value="{ThemeResource BodyLargeFontWeight}" />
		<Setter Property="CharacterSpacing" Value="{ThemeResource BodyLargeCharacterSpacing}" />
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="Foreground" Value="{ThemeResource CupertinoLabelBrush}" />
		<Setter Property="HorizontalContentAlignment" Value="Left" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="Padding" Value="{ThemeResource Space125Thickness}" />
		<Setter Property="InputScope" Value="Number" />
		<Setter Property="PlaceholderForeground" Value="{ThemeResource CupertinoPlaceholderTextBrush}" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="TextBox">
					<Grid x:Name="Root">

						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />
								<VisualState x:Name="PointerOver" />
								<VisualState x:Name="Pressed" />
								<VisualState x:Name="Disabled" />
								<VisualState x:Name="Focused" />
							</VisualStateGroup>
							<VisualStateGroup x:Name="ButtonStates">
								<VisualState x:Name="ButtonVisible" />
								<VisualState x:Name="ButtonCollapsed" />
							</VisualStateGroup>
							<VisualStateGroup x:Name="HeaderStates">
								<VisualState x:Name="NotEmpty" />
								<VisualState x:Name="Empty">
									<VisualState.Setters>
										<Setter Target="PlaceholderElement.Opacity" Value="1" />
									</VisualState.Setters>
									<VisualState.StateTriggers>
										<StateTrigger IsActive="{Binding Text, Converter={StaticResource EmptyToTrueConverter}, RelativeSource={RelativeSource TemplatedParent}}" />
									</VisualState.StateTriggers>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<Grid Margin="{TemplateBinding Padding}">
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="*" />
							</Grid.RowDefinitions>

							<ContentPresenter uc:CupertinoTypography.BindCharacterSpacing="True"
											  x:Name="HeaderContentPresenter"
											  Content="{TemplateBinding Header}"
											  ContentTemplate="{TemplateBinding HeaderTemplate}"
											  FontSize="{ThemeResource CupertinoHeaderFontSize}"
											  FontFamily="{ThemeResource CaptionLargeFontFamily}"
											  FontWeight="{ThemeResource CaptionLargeFontWeight}"
											  CharacterSpacing="{ThemeResource CaptionLargeCharacterSpacing}"
											  Foreground="{ThemeResource CupertinoHeaderForegroundBrush}"
											  TextWrapping="Wrap"
											  VerticalAlignment="Top" />

							<ScrollViewer x:Name="ContentElement"
										  Grid.Row="1"
										  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
										  HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
										  IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
										  IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}"
										  IsTabStop="False"
										  IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}"
										  VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
										  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
										  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
										  ZoomMode="Disabled"
										  AutomationProperties.AccessibilityView="Raw">
								<ScrollViewer.RenderTransform>
									<CompositeTransform x:Name="ContentElement_CompositeTransform" />
								</ScrollViewer.RenderTransform>
							</ScrollViewer>

							<TextBlock x:Name="PlaceholderElement"
									   Grid.Row="1"
									   Foreground="{TemplateBinding PlaceholderForeground}"
									   HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
									   IsHitTestVisible="False"
									   RenderTransformOrigin="0,0.5"
									   Text="{TemplateBinding PlaceholderText}"
									   TextAlignment="{TemplateBinding TextAlignment}"
									   TextWrapping="{TemplateBinding TextWrapping}"
									   VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
									   Opacity="0" />
						</Grid>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
